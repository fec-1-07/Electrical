const ac__abb__uncouples__S2C_A1 = defaultShape.extend({

  NAME: "ac__abb__uncouples__S2C_A1",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "../sites/default/files/styles/watermark/module/el_elements/elements/ac/abb/uncouples/S2C_A1/icon.png?itok=cTrEtV5N",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 50.00, radius: 5 },
    ];

    const setYb = [
      { value: 50.00, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const ac__abb__uncouples__S2C_A2 = defaultShape.extend({

  NAME: "ac__abb__uncouples__S2C_A2",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "../sites/default/files/styles/watermark/module/el_elements/elements/ac/abb/uncouples/S2C_A2/icon.png?itok=-vTH_I7s",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 50.00, radius: 5 },
    ];

    const setYb = [
      { value: 50.00, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const ac__DEKraft__VA_103__p1__2a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__2a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "../sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__DEKraft__VA_103__p1__6a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__6a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "../sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__DEKraft__VA_103__p1__10a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__10a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__DEKraft__VA_103__p1__16a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__16a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__DEKraft__VA_103__p1__20a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__20a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__DEKraft__VA_103__p1__25a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__25a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__DEKraft__VA_103__p1__32a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__32a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__DEKraft__VA_103__p1__40a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__40a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__DEKraft__VA_103__p1__50a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__50a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__DEKraft__VA_103__p1__63a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p1__63a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_1/icon.png?itok=2OvARIrV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 49.74, radius: 5},
    ];

    const setYb = [
      {value: 49.74, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__DEKraft__VA_103__p2__2a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__DEKraft__VA_103__p2__6a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__DEKraft__VA_103__p2__10a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__DEKraft__VA_103__p2__16a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__DEKraft__VA_103__p2__20a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__DEKraft__VA_103__p2__25a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__DEKraft__VA_103__p2__32a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__DEKraft__VA_103__p2__40a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__DEKraft__VA_103__p2__50a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__DEKraft__VA_103__p2__63a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_2/icon.png?itok=G_8NA_GB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},
      
    ];

    const setYb = [
      {value: 26.15, radius: 5},
      {value: 74.66, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__DEKraft__VA_103__p3__2a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "../sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__DEKraft__VA_103__p3__6a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__DEKraft__VA_103__p3__10a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__DEKraft__VA_103__p3__16a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__DEKraft__VA_103__p3__20a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__DEKraft__VA_103__p3__25a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__DEKraft__VA_103__p3__32a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__DEKraft__VA_103__p3__40a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__DEKraft__VA_103__p3__50a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__DEKraft__VA_103__p3__63a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_3/icon.png?itok=XvkXHo1c",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    const setYb = [
      {value: 17.79, radius: 5},
      {value: 50.27, radius: 5},
      {value: 82.76, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__DEKraft__VA_103__p4__2a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__DEKraft__VA_103__p4__6a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__DEKraft__VA_103__p4__10a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__DEKraft__VA_103__p4__16a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__DEKraft__VA_103__p4__20a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__DEKraft__VA_103__p4__25a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__DEKraft__VA_103__p4__32a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__DEKraft__VA_103__p4__40a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__DEKraft__VA_103__p4__50a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__DEKraft__VA_103__p4__63a = defaultShape.extend({

  NAME: "ac__DEKraft__VA_103__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/DEKraft%20/%D0%92%D0%90-103/pole_4/icon.png?itok=Y1fR74FY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    const setYb = [
      { value: 13.27, radius: 5 },
      { value: 37.89, radius: 5 },
      { value: 62.79, radius: 5 },
      { value: 87.28, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__ekf__averes__p1__2a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__ekf__averes__p1__6a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__ekf__averes__p1__10a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__ekf__averes__p1__16a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__ekf__averes__p1__20a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__ekf__averes__p1__25a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__ekf__averes__p1__32a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__ekf__averes__p1__40a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__ekf__averes__p1__50a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__ekf__averes__p1__63a = defaultShape.extend({

  NAME: "ac__ekf__averes__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_1/icon.png?itok=sJq8eprw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__ekf__averes__p2__2a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__averes__p2__6a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__averes__p2__10a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__averes__p2__16a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__averes__p2__20a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__averes__p2__25a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__averes__p2__32a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__averes__p2__40a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__averes__p2__50a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__averes__p2__63a = defaultShape.extend({

  NAME: "ac__ekf__averes__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_2/icon.png?itok=YkrzNTKp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__averes__p3__2a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__averes__p3__6a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__averes__p3__10a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__averes__p3__16a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__averes__p3__20a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__averes__p3__25a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__averes__p3__32a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__averes__p3__40a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__averes__p3__50a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__averes__p3__63a = defaultShape.extend({

  NAME: "ac__ekf__averes__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_3/icon.png?itok=Kf91ZdDz",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__averes__p4__2a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__ekf__averes__p4__6a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__ekf__averes__p4__10a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__ekf__averes__p4__16a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__ekf__averes__p4__20a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__ekf__averes__p4__25a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__ekf__averes__p4__32a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__ekf__averes__p4__40a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__ekf__averes__p4__50a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__ekf__averes__p4__63a = defaultShape.extend({

  NAME: "ac__ekf__averes__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/averes/pole_4/icon.png?itok=x9eBmpJS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__ekf__basic__p1__2a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__ekf__basic__p1__6a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__ekf__basic__p1__10a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__ekf__basic__p1__16a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__ekf__basic__p1__20a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__ekf__basic__p1__25a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__ekf__basic__p1__32a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__ekf__basic__p1__40a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__ekf__basic__p1__50a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__ekf__basic__p1__63a = defaultShape.extend({

  NAME: "ac__ekf__basic__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_1/icon.png?itok=H6-Rv_tO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__ekf__basic__p2__2a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__basic__p2__6a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__basic__p2__10a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__basic__p2__16a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__basic__p2__20a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__basic__p2__25a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__basic__p2__32a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__basic__p2__40a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__basic__p2__50a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__basic__p2__63a = defaultShape.extend({

  NAME: "ac__ekf__basic__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_2/icon.png?itok=hPdEwSOZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__basic__p3__2a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__basic__p3__6a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__basic__p3__10a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__basic__p3__16a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__basic__p3__20a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__basic__p3__25a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__basic__p3__32a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__basic__p3__40a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__basic__p3__50a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__basic__p3__63a = defaultShape.extend({

  NAME: "ac__ekf__basic__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_3/icon.png?itok=w5jmGq7Y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__basic__p4__2a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__ekf__basic__p4__6a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__ekf__basic__p4__10a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__ekf__basic__p4__16a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__ekf__basic__p4__20a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__ekf__basic__p4__25a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__ekf__basic__p4__32a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__ekf__basic__p4__40a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__ekf__basic__p4__50a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__ekf__basic__p4__63a = defaultShape.extend({

  NAME: "ac__ekf__basic__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/basic/pole_4/icon.png?itok=OyAHb71n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.54 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__ekf__proxima__p1__2a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__ekf__proxima__p1__6a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__ekf__proxima__p1__10a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__ekf__proxima__p1__16a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__ekf__proxima__p1__20a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__ekf__proxima__p1__25a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__ekf__proxima__p1__32a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__ekf__proxima__p1__40a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__ekf__proxima__p1__50a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__ekf__proxima__p1__63a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_1/icon.png?itok=SZppFaL4",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__ekf__proxima__p2__2a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__proxima__p2__6a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__proxima__p2__10a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__proxima__p2__16a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__proxima__p2__20a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__proxima__p2__25a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__proxima__p2__32a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__proxima__p2__40a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__proxima__p2__50a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__proxima__p2__63a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_2/icon.png?itok=OrH6CaPq",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__proxima__p3__2a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__ekf__proxima__p3__6a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__ekf__proxima__p3__10a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__ekf__proxima__p3__16a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__ekf__proxima__p3__20a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__ekf__proxima__p3__25a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__ekf__proxima__p3__32a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__ekf__proxima__p3__40a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__ekf__proxima__p3__50a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__ekf__proxima__p3__63a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_3/icon.png?itok=YyIT9g0H",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__ekf__proxima__p4__2a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__ekf__proxima__p4__6a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__ekf__proxima__p4__10a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__ekf__proxima__p4__16a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__ekf__proxima__p4__20a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__ekf__proxima__p4__25a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__ekf__proxima__p4__32a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__ekf__proxima__p4__40a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__ekf__proxima__p4__50a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__ekf__proxima__p4__63a = defaultShape.extend({

  NAME: "ac__ekf__proxima__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/ekf/proxima/pole_4/icon.png?itok=xiSOp0-j",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__iek__armat__p1__2a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__iek__armat__p1__6a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__iek__armat__p1__10a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__iek__armat__p1__13a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__13a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '13A'})
  },
});
 const ac__iek__armat__p1__16a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__iek__armat__p1__20a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__iek__armat__p1__25a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__iek__armat__p1__32a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__iek__armat__p1__40a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__iek__armat__p1__50a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__iek__armat__p1__63a = defaultShape.extend({

  NAME: "ac__iek__armat__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 85

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_1/icon.png?itok=-DZX2T_U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__iek__armat__p2__2a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__armat__p2__6a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__armat__p2__10a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__armat__p2__16a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__armat__p2__20a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__armat__p2__25a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__armat__p2__32a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__armat__p2__40a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__armat__p2__50a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__armat__p2__63a = defaultShape.extend({

  NAME: "ac__iek__armat__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_2/icon.png?itok=vJA18MmJ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__armat__p3__2a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__armat__p3__6a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__armat__p3__10a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__armat__p3__13a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__13a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '13A' })
  },
});
 const ac__iek__armat__p3__16a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__armat__p3__20a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__armat__p3__25a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__armat__p3__32a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__armat__p3__40a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__armat__p3__50a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__armat__p3__63a = defaultShape.extend({

  NAME: "ac__iek__armat__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_3/icon.png?itok=gRh0aR3n",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__armat__p4__2a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__iek__armat__p4__6a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__iek__armat__p4__10a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__iek__armat__p4__16a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__iek__armat__p4__20a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__iek__armat__p4__25a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__iek__armat__p4__32a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__iek__armat__p4__40a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__iek__armat__p4__50a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__iek__armat__p4__63a = defaultShape.extend({

  NAME: "ac__iek__armat__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/armat/pole_4/icon.png?itok=Xr81Zy6_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__iek__generica__p1__2a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__iek__generica__p1__6a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__iek__generica__p1__10a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__iek__generica__p1__16a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__iek__generica__p1__20a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__iek__generica__p1__25a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__iek__generica__p1__32a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__iek__generica__p1__40a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__iek__generica__p1__50a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__iek__generica__p1__63a = defaultShape.extend({

  NAME: "ac__iek__generica__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 82

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_1/icon.png?itok=-_A_Z1KI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__iek__generica__p2__2a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__generica__p2__6a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__generica__p2__10a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__generica__p2__16a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__generica__p2__20a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__generica__p2__25a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__generica__p2__32a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__generica__p2__40a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__generica__p2__50a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__generica__p2__63a = defaultShape.extend({

  NAME: "ac__iek__generica__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_2/icon.png?itok=O31088BO",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__generica__p3__2a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__generica__p3__6a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__generica__p3__10a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__generica__p3__16a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__generica__p3__20a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__generica__p3__25a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__generica__p3__32a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__generica__p3__40a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__generica__p3__50a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__generica__p3__63a = defaultShape.extend({

  NAME: "ac__iek__generica__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/generica/pole_3/icon.png?itok=OQRHXtJJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.49 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__karat__p1__2a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__2a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '2A'})
  },
});
 const ac__iek__karat__p1__6a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__6a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A'})
  },
});
 const ac__iek__karat__p1__10a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__10a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A'})
  },
});
 const ac__iek__karat__p1__16a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__16a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
  },
});
 const ac__iek__karat__p1__20a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__20a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A'})
  },
});
 const ac__iek__karat__p1__25a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__25a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
  },
});
 const ac__iek__karat__p1__32a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__32a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
  },
});
 const ac__iek__karat__p1__40a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__40a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
  },
});
 const ac__iek__karat__p1__50a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__50a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '50A'})
  },
});
 const ac__iek__karat__p1__63a = defaultShape.extend({

  NAME: "ac__iek__karat__p1__63a",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_1/icon.png?itok=MwJVKGWQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},
    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
  },
});
 const ac__iek__karat__p2__2a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__2a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__karat__p2__6a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__6a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__karat__p2__10a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__10a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__karat__p2__16a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__karat__p2__20a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__20a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__karat__p2__25a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__karat__p2__32a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__karat__p2__40a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__karat__p2__50a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__karat__p2__63a = defaultShape.extend({

  NAME: "ac__iek__karat__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_2/icon.png?itok=7HuXxwWz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__karat__p3__2a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__2a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })
  },
});
 const ac__iek__karat__p3__6a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__6a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
  },
});
 const ac__iek__karat__p3__10a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__10a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
  },
});
 const ac__iek__karat__p3__16a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const ac__iek__karat__p3__20a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__20a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const ac__iek__karat__p3__25a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const ac__iek__karat__p3__32a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const ac__iek__karat__p3__40a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const ac__iek__karat__p3__50a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const ac__iek__karat__p3__63a = defaultShape.extend({

  NAME: "ac__iek__karat__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_3/icon.png?itok=PjVns8sZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    const setYb = [
      { value: 16.7, radius: 5 },
      { value: 50, radius: 5 },
      { value: 83.3, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const ac__iek__karat__p4__2a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__2a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '2A' })

  },
});
 const ac__iek__karat__p4__6a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__6a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })

  },
});
 const ac__iek__karat__p4__10a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__10a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })

  },
});
 const ac__iek__karat__p4__16a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const ac__iek__karat__p4__20a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__20a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })

  },
});
 const ac__iek__karat__p4__25a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const ac__iek__karat__p4__32a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const ac__iek__karat__p4__40a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const ac__iek__karat__p4__50a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const ac__iek__karat__p4__63a = defaultShape.extend({

  NAME: "ac__iek__karat__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/iek/karat/pole_4/icon.png?itok=zK9inNoH",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.81 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const ac__meandr__BA9__p1__6a = defaultShape.extend({

  NAME: "ac__meandr__BA9__p1__6a",

  init: function (attr) {
    const realWidth = 9;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/meandr/BA9/pole_1/icon.png?itok=rpbrLiwY",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},

    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.82 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '6A', fontSize: 7 })
  },
});
 const ac__meandr__BA9__p1__10a = defaultShape.extend({

  NAME: "ac__meandr__BA9__p1__10a",

  init: function (attr) {
    const realWidth = 9;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/meandr/BA9/pole_1/icon.png?itok=rpbrLiwY",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},

    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.82 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '10A', fontSize: 7 })
  },
});
 const ac__meandr__BA9__p1__16a = defaultShape.extend({

  NAME: "ac__meandr__BA9__p1__16a",

  init: function (attr) {
    const realWidth = 9;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/meandr/BA9/pole_1/icon.png?itok=rpbrLiwY",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},

    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.82 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A', fontSize: 7 })
  },
});
 const ac__meandr__BA9__p1__20a = defaultShape.extend({

  NAME: "ac__meandr__BA9__p1__20a",

  init: function (attr) {
    const realWidth = 9;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ac/meandr/BA9/pole_1/icon.png?itok=rpbrLiwY",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 50, radius: 5},

    ];

    const setYb = [
      {value: 50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.82 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '20A', fontSize: 7 })
  },
});
 const contactor__dekraft__mk_103__16a__2 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__16a__2",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/16/02/icon.png?itok=j85u633x",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '02'})
  },
});
 const contactor__dekraft__mk_103__16a__11 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__16a__11",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/16/11/icon.png?itok=toMJyYSW",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '11'})
  },
});
 const contactor__dekraft__mk_103__16a__20 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__16a__20",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/16/20/icon.png?itok=Kg5oCJvN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '16A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '20'})
  },
});
 const contactor__dekraft__mk_103__25a__2 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__25a__2",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/25/02/icon.png?itok=JBELs-eL",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '02'})
  },
});
 const contactor__dekraft__mk_103__25a__11 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__25a__11",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/25/11/icon.png?itok=Btct8Fb_",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '11'})
  },
});
 const contactor__dekraft__mk_103__25a__20 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__25a__20",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/25/20/icon.png?itok=a9J_JOel",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    const setYb = [
      {value: 29.19, radius: 5},
      {value: 50.00, radius: 3},
      {value: 71.77, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '25A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '20'})
  },
});
 const contactor__dekraft__mk_103__32a__2 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__2",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/02/icon.png?itok=dYJc-Rpz",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '02'})
  },
});
 const contactor__dekraft__mk_103__32a__4 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__4",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/04/icon.png?itok=3ttXAkhf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
    Utils.createLabel(this, { x: 0, y: y2, text: '04' })
  },
});
 const contactor__dekraft__mk_103__32a__11 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__11",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/11/icon.png?itok=572nlxIX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '11'})
  },
});
 const contactor__dekraft__mk_103__32a__20 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__20",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/20/icon.png?itok=KYfxh7F2",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '20'})
  },
});
 const contactor__dekraft__mk_103__32a__22 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__22",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/22/icon.png?itok=39DHlFGy",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '22'})
  },
});
 const contactor__dekraft__mk_103__32a__31 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__31",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/31/icon.png?itok=UKNOSSiG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '31'})
  },
});
 const contactor__dekraft__mk_103__32a__40 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__32a__40",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/32/40/icon.png?itok=wqKthUsg",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '32A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '40'})
  },
});
 const contactor__dekraft__mk_103__40a__2 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__2",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/02/icon.png?itok=yek7_W8m",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '02'})
  },
});
 const contactor__dekraft__mk_103__40a__4 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__4",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/04/icon.png?itok=4sb1sncE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
    Utils.createLabel(this, { x: 0, y: y2, text: '04' })
  },
});
 const contactor__dekraft__mk_103__40a__11 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__11",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/11/icon.png?itok=kxZEOBRg",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '11'})
  },
});
 const contactor__dekraft__mk_103__40a__20 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__20",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/20/icon.png?itok=aCQwDd71",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '20'})
  },
});
 const contactor__dekraft__mk_103__40a__22 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__22",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/22/icon.png?itok=3XyJK5Td",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '22'})
  },
});
 const contactor__dekraft__mk_103__40a__31 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__31",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/31/icon.png?itok=uJDky37t",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '31'})
  },
});
 const contactor__dekraft__mk_103__40a__40 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__40a__40",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/40/40/icon.png?itok=fxwRAkdo",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '40A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '40'})
  },
});
 const contactor__dekraft__mk_103__63a__2 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__2",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/02/icon.png?itok=vy1pGZu9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '02'})
  },
});
 const contactor__dekraft__mk_103__63a__4 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__4",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/04/icon.png?itok=jyrB4UjI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
    Utils.createLabel(this, { x: 0, y: y2, text: '04' })
  },
});
 const contactor__dekraft__mk_103__63a__11 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__11",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/11/icon.png?itok=gtcoClex",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '11'})
  },
});
 const contactor__dekraft__mk_103__63a__20 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__20",

  init: function (attr) {
    const realWidth = 17.5 * 2;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/20/icon.png?itok=ZC4g3gD1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    const setYb = [
      { value: 26.87, radius: 5 },
      { value: 54.85, radius: 5 },
      { value: 86.70, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '20'})
  },
});
 const contactor__dekraft__mk_103__63a__22 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__22",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/22/icon.png?itok=iWa4gZu2",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '22'})
  },
});
 const contactor__dekraft__mk_103__63a__31 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__31",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/31/icon.png?itok=UKKQ5eoQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '31'})
  },
});
 const contactor__dekraft__mk_103__63a__40 = defaultShape.extend({

  NAME: "contactor__dekraft__mk_103__63a__40",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 80;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/DEKraft/mk_103/63/40/icon.png?itok=BTD6XdDG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 13.25, radius: 5 },
      { value: 31.40, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.51, radius: 5 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    const y2 = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: '63A'})
    Utils.createLabel(this, {x: 0, y: y2, text: '40'})
  },
});
 const contactor__ekf__km_4__16a = defaultShape.extend({

  NAME: "contactor__ekf__km_4__16a",

  init: function (attr) {
    const realWidth = 60;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/EKF/km-4/icon.png?itok=4kQezUeF",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    const setYb = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.33 * realWidth * Utils.sizeCoef;
    const y = 0.55 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'КМ-16', fontSize: 10 })
  },
});
 const contactor__ekf__km_4__25a = defaultShape.extend({

  NAME: "contactor__ekf__km_4__25a",

  init: function (attr) {
    const realWidth = 60;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/EKF/km-4/icon.png?itok=4kQezUeF",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    const setYb = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.33 * realWidth * Utils.sizeCoef;
    const y = 0.55 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'КМ-25', fontSize: 10 })
  },
});
 const contactor__ekf__km_4__32a = defaultShape.extend({

  NAME: "contactor__ekf__km_4__32a",

  init: function (attr) {
    const realWidth = 60;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/EKF/km-4/icon.png?itok=4kQezUeF",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    const setYb = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.33 * realWidth * Utils.sizeCoef;
    const y = 0.55 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'КМ-32', fontSize: 10 })
  },
});
 const contactor__ekf__km_4__40a = defaultShape.extend({

  NAME: "contactor__ekf__km_4__40a",

  init: function (attr) {
    const realWidth = 60;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/EKF/km-4/icon.png?itok=4kQezUeF",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    const setYb = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.33 * realWidth * Utils.sizeCoef;
    const y = 0.55 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'КМ-40', fontSize: 10 })
  },
});
 const contactor__ekf__km_4__50a = defaultShape.extend({

  NAME: "contactor__ekf__km_4__50a",

  init: function (attr) {
    const realWidth = 60;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/EKF/km-4/icon.png?itok=4kQezUeF",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    const setYb = [
      { value: 13.35, radius: 5 },
      { value: 31.26, radius: 5 },
      { value: 49.36, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 88.67, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.33 * realWidth * Utils.sizeCoef;
    const y = 0.55 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'КМ-50', fontSize: 10 })
  },
});
 const contactor__iek__km__40a__11m = defaultShape.extend({

  NAME: "contactor__iek__km__40a__11m",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/km/icon.png?itok=NOSK5lG4",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    const setYb = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.1 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'КМ40-11М', fontSize: 8 })
  },
});
 const contactor__iek__km__40a__20m = defaultShape.extend({

  NAME: "contactor__iek__km__40a__20m",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/km/icon.png?itok=NOSK5lG4",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    const setYb = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.1 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'КМ40-20М', fontSize: 8 })
  },
});
 const contactor__iek__km__63a__11m = defaultShape.extend({

  NAME: "contactor__iek__km__63a__11m",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/km/icon.png?itok=NOSK5lG4",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    const setYb = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.1 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'КМ63-11М', fontSize: 8 })
  },
});
 const contactor__iek__km__63a__20m = defaultShape.extend({

  NAME: "contactor__iek__km__63a__20m",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/km/icon.png?itok=NOSK5lG4",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    const setYb = [
      {value: 29.40, radius: 5},
      {value: 60.10, radius: 5},
      {value: 86.61, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.1 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'КМ63-20М', fontSize: 8 })
  },
});
 const contactor__iek__kmi__24v__9a = defaultShape.extend({

  NAME: "contactor__iek__kmi__24v__9a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '24В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '9A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__24v__12a = defaultShape.extend({

  NAME: "contactor__iek__kmi__24v__12a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '24В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '12A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__24v__18a = defaultShape.extend({

  NAME: "contactor__iek__kmi__24v__18a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '24В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '18A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__24v__25a = defaultShape.extend({

  NAME: "contactor__iek__kmi__24v__25a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '24В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '25A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__24v__32a = defaultShape.extend({

  NAME: "contactor__iek__kmi__24v__32a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '24В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '32A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__36v__9a = defaultShape.extend({

  NAME: "contactor__iek__kmi__36v__9a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '36В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '9A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__36v__12a = defaultShape.extend({

  NAME: "contactor__iek__kmi__36v__12a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '36В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '12A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__36v__18a = defaultShape.extend({

  NAME: "contactor__iek__kmi__36v__18a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '36В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '18A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__36v__32a = defaultShape.extend({

  NAME: "contactor__iek__kmi__36v__32a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '36В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '32A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__36v__40a = defaultShape.extend({

  NAME: "contactor__iek__kmi__36v__40a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '36В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '40A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__9a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__9a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '100В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '9A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__12a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__12a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '12A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__18a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__18a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '18A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__32a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__32a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '32A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__40a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__40a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '40A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__50a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__50a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '50A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__65a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__65a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '65A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__80a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__80a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '80A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__110v__90a = defaultShape.extend({

  NAME: "contactor__iek__kmi__110v__90a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '110В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '90A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__9a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__9a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '9A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__12a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__12a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '12A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__18a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__18a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '18A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__32a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__32a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '32A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__40a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__40a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '40A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__50a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__50a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '50A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__65a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__65a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '65A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__80a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__80a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '80A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__230v__90a = defaultShape.extend({

  NAME: "contactor__iek__kmi__230v__90a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '230В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '90A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__9a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__9a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '9A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__12a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__12a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '12A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__18a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__18a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '18A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__32a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__32a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '32A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__40a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__40a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '40A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__50a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__50a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '50A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__65a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__65a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '65A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__80a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__80a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '80A', fontSize: 8 })
  },
});
 const contactor__iek__kmi__400v__90a = defaultShape.extend({

  NAME: "contactor__iek__kmi__400v__90a",

  init: function (attr) {
    const realWidth = 53; //53
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/contactor/iek/kmi/icon.png?itok=EAskFXSX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.06, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.30, radius: 5 },
      { value: 61.06, radius: 5 },
      { value: 81.06, radius: 5 },
      { value: 90.72, radius: 3 },
    ];

    const setYb = [
      { value: 9.21, radius: 3 },
      { value: 18.51, radius: 5 },
      { value: 38.51, radius: 5 },
      { value: 55.02, radius: 3 },
      { value: 64.26, radius: 5 },
      { value: 84.47, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = -0.1 * realWidth * Utils.sizeCoef;
    const x2 = 0.02 * realWidth * Utils.sizeCoef;
    const x3 = 0.15 * realWidth * Utils.sizeCoef;
    const y = 0.77 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: '400В', fontSize: 8 })
    Utils.createLabel(this, { x: x2, y, text: '/', fontSize: 8 })
    Utils.createLabel(this, { x: x3, y, text: '90A', fontSize: 8 })
  },
});
 const dif__ekf__averes__dva_6__p2__1a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__1a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '1A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__6a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__6a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__6a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__6a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__6a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__6a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__10a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__10a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__10a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__10a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__10a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__10a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__10a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__10a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__13a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__13a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '13A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__13a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__13a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '13A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__13a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__13a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '13A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__13a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__13a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '13A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__16a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__16a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__16a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__16a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__16a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__16a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__16a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__16a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__20a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__20a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__20a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__20a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__20a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__20a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__20a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__20a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__25a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__25a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__25a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__25a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__25a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__25a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__25a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__25a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__32a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__32a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__32a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__32a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__32a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__32a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__32a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__32a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__40a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__40a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__40a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__40a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__40a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__40a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__40a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__40a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__50a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__50a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__50a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__50a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__50a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__50a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__50a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__50a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__63a__10ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__63a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__63a__30ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__63a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__63a__100ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__63a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__ekf__averes__dva_6__p2__63a__300ma = defaultShape.extend({

  NAME: "dif__ekf__averes__dva_6__p2__63a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/AVERES/DVA_6/pole_2/icon.png?itok=Rm_MiG29",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__ekf__proxima__p1__abdt_63m__6a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__6a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '6A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__6a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__6a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '6A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__6a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__6a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '6A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__10a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__10a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__10a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__10a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__10a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__10a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__16a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__16a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__16a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__16a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__16a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__16a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__20a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__20a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__20a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__20a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__20a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__20a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__25a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__25a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__25a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__25a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__25a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__25a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__32a__10ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__32a__10ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '10mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__32a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__32a__30ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__abdt_63m__32a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__abdt_63m__32a__100ma",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/ABDT_63%D0%9C/icon.png?itok=rV6Dx9LE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    const setYb = [
      { value: 26.98, radius: 5 },
      { value: 72.56, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.55 * realHeight * Utils.sizeCoef;
    const x2 = 0.1 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__6a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__6a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__6a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__6a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__6a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__6a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__10a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__10a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__10a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__10a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__10a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__10a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__16a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__16a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__16a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__16a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__16a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__16a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__20a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__20a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__20a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__20a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__20a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__20a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__25a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__25a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__25a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__25a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__25a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__25a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__32a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__32a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__32a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__32a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__32a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__32a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__40a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__40a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__40a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__40a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__40a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__40a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__50a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__50a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__50a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__50a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__50a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__50a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__63a__30ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__63a__30ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__63a__100ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__63a__100ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});

 const dif__ekf__proxima__p1__ad1__63a__300ma = defaultShape.extend({

  NAME: "dif__ekf__proxima__p1__ad1__63a__300ma",

  init: function (attr) {
    const realWidth = 71.6;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/EKF/proxima/pole_1/AD1/icon.png?itok=LRQ65QO9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 60.77, radius: 5 },
      { value: 86.17, radius: 5 },
    ];

    const setYb = [
      { value: 10.90, radius: 5 },
      { value: 29.39, radius: 5 },
      { value: 60.51, radius: 5 },
      { value: 85.90, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    const x = 0.3 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});

 const dif__iek__armat__p2__10a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__10a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__10a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__10a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__10a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__10a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__10a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__10a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__10a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__10a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__16a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__16a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__16a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__16a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__16a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__20a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__20a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__20a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__20a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__20a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__25a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__25a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__25a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__25a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__25a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__32a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__32a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__32a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__32a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__32a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__40a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__40a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__40a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__40a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__40a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__63a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__63a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__63a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__63a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__63a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__80a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__80a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__80a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__80a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__80a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__100a__10ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__100a__30ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__100a__100ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__100a__300ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__armat__p2__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__armat__p2__100a__500ma",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/armat/pole_2/icon.png?itok=iN-HX-hB",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.9 * realHeight * Utils.sizeCoef;
    const y2 = 0.25 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__generica__p2__10a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__10a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__10a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__10a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__10a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__10a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__10a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__10a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__10a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__10a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__16a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__16a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__16a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__16a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__16a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__20a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__20a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__20a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__20a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__20a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__25a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__25a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__25a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__25a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__25a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__32a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__32a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__32a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__32a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__32a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__40a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__40a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__40a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__40a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__40a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__63a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__63a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__63a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__63a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__63a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__80a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__80a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__80a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__80a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__80a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__100a__10ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__100a__30ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__100a__100ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__100a__300ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p2__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p2__100a__500ma",

  init: function (attr) {
    const din = 3.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_2/icon.png?itok=A2bzzc7L",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 56.64, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    const setYb = [
      { value: 8.57, radius: 5 },
      { value: 23.03, radius: 5 },
      { value: 56.30, radius: 5 },
      { value: 85.38, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__10a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__10a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__10a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__10a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__10a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__10a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__10a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__10a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__10a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__10a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__16a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__16a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__16a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__16a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__16a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__20a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__20a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__20a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__20a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__20a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__25a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__25a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__25a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__25a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__25a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__32a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__32a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__32a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__32a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__32a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__40a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__40a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__40a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__40a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__40a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__63a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__63a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__63a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__63a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__63a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__80a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__80a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__80a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__80a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__80a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__100a__10ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__100a__30ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__100a__100ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__100a__300ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__generica__p4__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__generica__p4__100a__500ma",

  init: function (attr) {
    const din = 6.5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/generica/pole_4/icon.png?itok=528Tkw33",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 45.90, radius: 5 },
      { value: 61.32, radius: 5 },
      { value: 76.65, radius: 5 },
      { value: 92.06, radius: 5 },
    ];

    const setYb = [
      { value: 4.78, radius: 5 },
      { value: 12.53, radius: 5 },
      { value: 20.29, radius: 5 },
      { value: 27.86, radius: 5 },
      { value: 45.72, radius: 5 },
      { value: 61.05, radius: 5 },
      { value: 76.56, radius: 5 },
      { value: 91.97, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.43 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__6a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__6a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '6A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__10a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__10a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__16a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__16a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__20a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__25a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__25a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__32a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__32a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__40a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '340A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__40a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__50a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__50a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__abdt32__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__abdt32__63a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/ABDT32/icon.png?itok=34v4Ithi",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    const setYb = [
      { value: 25.61, radius: 5 },
      { value: 74.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.24 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__16a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__16a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__16a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__16a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__16a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__20a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__20a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__20a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__20a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__20a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__25a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__25a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__25a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__25a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__25a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__32a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__32a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__32a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__32a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__32a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__40a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__40a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__40a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__40a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__40a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__63a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__63a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__63a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__63a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__63a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__80a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__80a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__80a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__80a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__80a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__100a__10ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__100a__30ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__100a__100ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__100a__300ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p2__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p2__100a__500ma",

  init: function (attr) {
    const din = 3;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_2/icon.png?itok=pzwmhB7O",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    const setYb = [
      { value: 50.29, radius: 5 },
      { value: 83.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3_n__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__16a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__16a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__16a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__16a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__16a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__20a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__20a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__20a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__20a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__20a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__25a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__25a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__25a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__25a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__25a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__32a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__32a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__32a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__32a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__32a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__40a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__40a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__40a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__40a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__40a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__63a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__63a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__63a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__63a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__63a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__80a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__80a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__80a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__80a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__80a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__100a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__100a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__100a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__100a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3_n__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3_n__100a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_3/icon.png?itok=5daBmLVd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const dif__iek__karat__p3__16a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__16a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__16a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__16a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__16a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__16a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__16a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__16a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__16a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__16a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__20a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__20a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__20a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__20a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__20a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__20a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__20a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__20a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__20a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__20a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__25a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__25a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__25a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__25a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__25a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__25a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__25a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__25a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__25a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__25a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__32a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__32a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__32a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__32a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__32a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__32a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__32a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__32a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__32a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__32a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__40a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__40a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__40a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__40a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__40a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__40a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__40a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__40a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__40a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__40a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__63a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__63a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__63a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__63a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__63a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__63a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__63a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__63a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__63a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__63a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__80a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__80a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__80a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__80a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__80a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__80a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__80a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__80a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__80a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__80a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__100a__10ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__100a__10ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '10mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__100a__30ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__100a__30ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '30mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__100a__100ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__100a__100ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '100mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__100a__300ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__100a__300ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '300mA', fontSize: 8 })
  },
});
 const dif__iek__karat__p3__100a__500ma = defaultShape.extend({

  NAME: "dif__iek__karat__p3__100a__500ma",

  init: function (attr) {
    const din = 5;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/dif/iek/karat/pole_4/icon.png?itok=h0IO57cp",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    const setYb = [
      { value: 30.35, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 70.00, radius: 5 },
      { value: 89.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.85 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din - 3) / 2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x: x2, y, text: '500mA', fontSize: 8 })
  },
});
 const electricityMeters__ART_02PQRSIN = defaultShape.extend({

  NAME: "electricityMeters__ART_02PQRSIN",

  init: function (attr) {
    const realWidth = 170;
    const realHeight = 213;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/electricityMeters/Merkyriy/ART_02PQRSIN/icon.png?itok=arSV0gjt",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 16.76, radius: 5},
      {value: 26.23, radius: 5},
      {value: 35.79, radius: 5},
      {value: 45.18, radius: 5},
      {value: 54.74, radius: 5},
      {value: 64.29, radius: 5},
      {value: 73.77, radius: 5},
      {value: 83.16, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const electricityMeters__Merkyriy__Merkyriy_200 = defaultShape.extend({

  NAME: "electricityMeters__Merkyriy__Merkyriy_200",

  init: function (attr) {
    const realWidth = 156;
    const realHeight = 138;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/electricityMeters/Merkyriy/Merkyriy_200/icon.png?itok=-b9W-G6I",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.27, radius: 3},
      {value: 22.92, radius: 3},
      {value: 43.79, radius: 5},
      {value: 50.73, radius: 5},
      {value: 57.86, radius: 5},
      {value: 64.93, radius: 5},
      {value: 74.28, radius: 3},
      {value: 76.70, radius: 3},
      {value: 79.06, radius: 3},
      {value: 81.41, radius: 3},
      {value: 83.77, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const electricityMeters__Merkyriy_201_6 = defaultShape.extend({

  NAME: "electricityMeters__Merkyriy_201_6",

  init: function (attr) {
    const realWidth = 105;
    const realHeight = 105;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/electricityMeters/Merkyriy/Merkyriy_201_6/icon.png?itok=N3KFocPq",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 20, radius: 5},
      {value: 33, radius: 5},
      {value: 67, radius: 5},
      {value: 80, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__cross__ekf__PROxima__415 = defaultShape.extend({

  NAME: "other__cross__ekf__PROxima__415",

  init: function (attr) {
    const realWidth = 132;
    const realHeight = 87;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/EKF/PROxima/4_15/icon.png?itok=QTwjkbtQ",
      realWidth,
      realHeight
    }, attr));

    const setY = [
      { value: 5.35, radius: 5 },
      { value: 11.76, radius: 5 },
      { value: 18.16, radius: 5 },
      { value: 24.49, radius: 5 },
      { value: 30.90, radius: 5 },
      { value: 37.23, radius: 5 },
      { value: 43.63, radius: 5 },
      { value: 50.04, radius: 5 },
      { value: 56.37, radius: 5 },
      { value: 62.70, radius: 5 },
      { value: 69.10, radius: 5 },
      { value: 75.51, radius: 5 },
      { value: 81.84, radius: 5 },
      { value: 88.24, radius: 5 },
      { value: 94.65, radius: 5 },
    ];

    const heightKoef = realHeight * Utils.sizeCoef / 100;
    const widthKoef = realWidth * Utils.sizeCoef / 100;


    const x1 = 9.12 * widthKoef;
    const x2 = 32.83 * widthKoef;
    const x3 = 63.39 * widthKoef;
    const x4 = 89.97 * widthKoef;

    for (let n in setY) {
      const radius = (setY[n].hasOwnProperty('radius')) ? setY[n].radius * Utils.sizeCoef : Utils.defaultRadius * Utils.sizeCoef;
      Utils.createPort(this, {
        locatorAttr: { x: x1, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x2, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x3, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x4, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
    }

  },
});
 const other__cross__ekf__PROxima__KBR_80A = defaultShape.extend({

  NAME: "other__cross__ekf__PROxima__KBR_80A",

  init: function (attr) {
    const realWidth = 30;
    const realHeight = 65;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/EKF/PROxima/KBR_80A/icon.png?itok=plHHIhxh",
      realWidth,
      realHeight,
    }, attr));


    const setY1 = [
      { value: 24.56, radius: 5 },
      { value: 49.56, radius: 5 },
      { value: 75.44, radius: 5 },
    ];

    const setY2 = [
      { value: 20.18, radius: 3 },
      { value: 39.91, radius: 3 },
      { value: 59.65, radius: 3 },
      { value: 79.39, radius: 3 },
    ];

    const yt1x = realWidth * Utils.sizeCoef * 54 / 100;
    const yb1x = realWidth * Utils.sizeCoef * 69 / 100;

    Utils.createSetOfPortsCustomX(this, setY1, yt1x, realWidth, realHeight, 0);

    Utils.createSetOfPortsCustomX(this, setY2, yb1x, realWidth, realHeight, 2);

  },
});
 const other__cross__iek__rbd_160a = defaultShape.extend({

  NAME: "other__cross__iek__rbd_160a",

  init: function (attr) {
    const realWidth = 28;
    const realHeight = 74.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/IEK/RBD_160%D0%90/icon.png?itok=MnlV0eLt",
      realWidth,
      realHeight,
    }, attr));


    const setY1 = [{value: 66.82, radius: 5},];
    const setY2 = [{value: 22.43, radius: 5}];

    const setY3 = [
      { value: 22.43, radius: 5 },
      { value: 49.07, radius: 5 },
      { value: 77.57, radius: 5 },
    ];

    const setY4 = [
      { value: 22.43, radius: 5 },
      { value: 49.07, radius: 5 },
      { value: 77.57, radius: 5 },
    ];

    const yt1x = realWidth * Utils.sizeCoef * 29.66 / 100;
    const yb2x = realWidth * Utils.sizeCoef * 26.82 / 100;
    const yt3x = realWidth * Utils.sizeCoef * 53 / 100;
    const yb4x = realWidth * Utils.sizeCoef * 66.6 / 100;

    Utils.createSetOfPortsCustomX(this, setY1, yt1x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setY2, yb2x, realWidth, realHeight, 0);

    Utils.createSetOfPortsCustomX(this, setY3, yt3x, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setY4, yb4x, realWidth, realHeight, 2);

  },
});
 const other__cross__iek__SNK_2_15 = defaultShape.extend({

  NAME: "other__cross__iek__SNK_2_15",

  init: function (attr) {
    const realWidth = 132;
    const realHeight = 45;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/IEK/SNK_2_15/icon.png?itok=4dujB5Cq",
      realWidth,
      realHeight,
    }, attr));

    const setY = [
      { value: 8.73, radius: 5 },
      { value: 14.55, radius: 5 },
      { value: 20.55, radius: 5 },
      { value: 26.46, radius: 5 },
      { value: 32.36, radius: 5 },
      { value: 38.27, radius: 5 },
      { value: 44.18, radius: 5 },
      { value: 50.09, radius: 5 },
      { value: 55.99, radius: 5 },
      { value: 61.99, radius: 5 },
      { value: 67.89, radius: 5 },
      { value: 73.80, radius: 5 },
      { value: 79.71, radius: 5 },
      { value: 85.62, radius: 5 },
      { value: 91.61, radius: 5 },
    ];

    const heightKoef = realHeight * Utils.sizeCoef / 100;
    const widthKoef = realWidth * Utils.sizeCoef / 100;

    const x1 = 23.4 * widthKoef;
    const x2 = 75.4 * widthKoef;

    for (let n in setY) {
      const radius = (setY[n].hasOwnProperty('radius')) ? setY[n].radius * Utils.sizeCoef : Utils.defaultRadius * Utils.sizeCoef;
      Utils.createPort(this, {
        locatorAttr: { x: x1, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x2, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
    }
  },
});
 const other__cross__iek__SNK_4_7 = defaultShape.extend({

  NAME: "other__cross__iek__SNK_4_7",

  init: function (attr) {
    const realWidth = 65;
    const realHeight = 87;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/IEK/SNK_4_7/icon.png?itok=mSpbVyK9",
      realWidth,
      realHeight,
    }, attr));

    const setY = [
      { value: 10.86, radius: 5 },
      { value: 24.18, radius: 5 },
      { value: 37.50, radius: 5 },
      { value: 50.82, radius: 5 },
      { value: 64.14, radius: 5 },
      { value: 77.66, radius: 5 },
      { value: 89.34, radius: 5 },
    ];

    const heightKoef = realHeight * Utils.sizeCoef / 100;
    const widthKoef = realWidth * Utils.sizeCoef / 100;

    const x1 = 9 * widthKoef;
    const x2 = 33.5 * widthKoef;
    const x3 = 65 * widthKoef;
    const x4 = 92 * widthKoef;

    for (let n in setY) {
      const radius = (setY[n].hasOwnProperty('radius')) ? setY[n].radius * Utils.sizeCoef : Utils.defaultRadius * Utils.sizeCoef;
      Utils.createPort(this, {
        locatorAttr: { x: x1, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x2, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x3, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x4, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
    }
  },
});
 const other__cross__iek__SNK_4_11 = defaultShape.extend({

  NAME: "other__cross__iek__SNK_4_11",

  init: function (attr) {
    const realWidth = 100;
    const realHeight = 87;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/cross/IEK/SNK_4_11/icon.png?itok=kyPeY8CY",
      realWidth,
      realHeight,
    }, attr));

    const setY = [
      { value: 8.39, radius: 5 },
      { value: 15.98, radius: 5 },
      { value: 24.63, radius: 5 },
      { value: 33.29, radius: 5 },
      { value: 41.94, radius: 5 },
      { value: 50.73, radius: 5 },
      { value: 59.39, radius: 5 },
      { value: 68.04, radius: 5 },
      { value: 76.70, radius: 5 },
      { value: 85.49, radius: 5 },
      { value: 92.94, radius: 5 },
    ];

    const heightKoef = realHeight * Utils.sizeCoef / 100;
    const widthKoef = realWidth * Utils.sizeCoef / 100;

    const x1 = 9 * widthKoef;
    const x2 = 33.5 * widthKoef;
    const x3 = 65 * widthKoef;
    const x4 = 92 * widthKoef;

    for (let n in setY) {
      const radius = (setY[n].hasOwnProperty('radius')) ? setY[n].radius * Utils.sizeCoef : Utils.defaultRadius * Utils.sizeCoef;
      Utils.createPort(this, {
        locatorAttr: { x: x1, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x2, y: heightKoef * setY[n].value },
        direction: 0,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x3, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
      Utils.createPort(this, {
        locatorAttr: { x: x4, y: heightKoef * setY[n].value },
        direction: 2,
        width: radius,
        height: radius
      });
    }
  },
});
 const other__lamps__EKF__PROxima__LSF_47 = defaultShape.extend({

  NAME: "other__lamps__EKF__PROxima__LSF_47",

  init: function (attr) {
    const realWidth = 9;
    const realHeight = 78;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/lamps/EKF/PROxima/LSF_47/icon.png?itok=ZU4PIG3X",
      realWidth,
      realHeight,
    }, attr));


    const setY = [{ value: 50, radius: 3 }];

    const yt1x = realWidth * Utils.sizeCoef * 4 / 100;
    const yt2x = realWidth * Utils.sizeCoef * 11 / 100;
    const yt3x = realWidth * Utils.sizeCoef * 18 / 100;

    const yb1x = realWidth * Utils.sizeCoef * 82 / 100;
    const yb2x = realWidth * Utils.sizeCoef * 89 / 100;
    const yb3x = realWidth * Utils.sizeCoef * 196 / 100;

    Utils.createSetOfPortsCustomX(this, setY, yt1x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setY, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setY, yt3x, realWidth, realHeight, 0);

    Utils.createSetOfPortsCustomX(this, setY, yb1x, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setY, yb2x, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setY, yb3x, realWidth, realHeight, 2);

  },
});
 const other__lamps__EKF__PROxima__LSF_47_type_2 = defaultShape.extend({

  NAME: "other__lamps__EKF__PROxima__LSF_47_type_2",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 84;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/lamps/EKF/PROxima/LSF_47_type_2/icon.png?itok=jUC3qcNV",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 33, radius: 3},
      {value: 66, radius: 3},
    ];

    const setYb = [
      {value: 33, radius: 3},
      {value: 66, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__meters__KTEM07 = defaultShape.extend({

  NAME: "other__meters__KTEM07",

  init: function (attr) {
    const realWidth = 37;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/KTEM07/icon.png?itok=KRrH6eQw",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 22.28, radius: 5 },
      { value: 77.45, radius: 5 },
    ];

    const setYb = [
      { value: 22.28, radius: 5 },
      { value: 77.45, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__meters__meandr__VAR_MOZ = defaultShape.extend({

  NAME: "other__meters__meandr__VAR_MOZ",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 93;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/meandr/BAP-MO3/icon.png?itok=EH23i86_",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.74, radius: 5 },
      { value: 71.12, radius: 5 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 9 / 100;

    const setYt2 = [
      { value: 26.74, radius: 5 },
      { value: 71.12, radius: 5 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 21 / 100;

    const setYb2 = [
      { value: 26.74, radius: 5 },
      { value: 71.12, radius: 5 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 91 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
  },
});
 const other__meters__meandr__VAR_MO1 = defaultShape.extend({

  NAME: "other__meters__meandr__VAR_MO1",

  init: function (attr) {
    const realWidth = 17.5;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/meandr/BAP_M01/icon.png?itok=X54m8cGF",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50, radius: 5 },
    ];

    const setYb = [
      { value: 50, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__meters__meandr__BAP_M01_08 = defaultShape.extend({

  NAME: "other__meters__meandr__BAP_M01_08",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/meandr/BAP_M01_08/icon.png?itok=9VVqr7i8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 61.42, radius: 3},
      {value: 80.90, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);

    const y = 0.85 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'BAP-M01-08', fontSize: 10 })
  },
});
 const other__meters__meandr__BAP_M02 = defaultShape.extend({

  NAME: "other__meters__meandr__BAP_M02",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/meandr/BAP_M02/icon.png?itok=IIre4K9V",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 24.19, radius: 3},
      {value: 50.54, radius: 3},
      {value: 75.81, radius: 3},
    ];

    const setYb = [
      {value: 24.19, radius: 3},
      {value: 75.81, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__meters__meandr__VAR_MOZ_1 = defaultShape.extend({

  NAME: "other__meters__meandr__VAR_MOZ_1",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 93;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/meandr/BAP_MO3_1/icon.png?itok=0_nmjHHF",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 26.20, radius: 5 },
      { value: 71.66, radius: 5 },
    ];

    const setYb = [
      { value: 26.20, radius: 5 },
      { value: 71.66, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const other__meters__rbuz__va_1_red = defaultShape.extend({

  NAME: "other__meters__rbuz__va_1_red",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/other/VA/RBUZ/VA_1_red/icon.png?itok=T_cBwxLy",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const ovr__Schneider_Electric__A9L16294 = defaultShape.extend({

  NAME: "ovr__Schneider_Electric__A9L16294",

  init: function (attr) {
    const realWidth = 150;
    const realHeight = 110;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovr/Schneider_Electric/A9L16294/icon.png?itok=OUov7q7L",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 60.82, radius: 5 },
      { value: 76.44, radius: 5 },
      { value: 91.99, radius: 5 },

      { value: 44.01, radius: 3 },
      { value: 47.98, radius: 3 },
      { value: 51.95, radius: 3 },
    ];

    const setYb = [
      { value: 5.76, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const ovr__Streamer__rif_3_II_275_20_3_1 = defaultShape.extend({

  NAME: "ovr__Streamer__rif_3_II_275_20_3_1",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovr/streamer/rif_3_II_275_20_3_1/icon.png?itok=piXdTHD0",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.68, radius: 5 },
      { value: 38.03, radius: 5 },
      { value: 61.97, radius: 5 },
      { value: 86.32, radius: 5 },
    ];

    const setYb = [
      { value: 50, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const ovrs__ecolite__YZIS_С1_40 = defaultShape.extend({

  NAME: "ovrs__ecolite__YZIS_С1_40",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 85.6;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/ecolite/YZIS_%D0%A11_40/icon.png?itok=wplFWP-y",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.48, radius: 5 },
      { value: 74.52, radius: 5 },
    ];

    const setYb = [
      { value: 25.48, radius: 5 },
      { value: 74.52, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const ovrs__EKF__afdd__10a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__10a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '10A'})
  },
});
 const ovrs__EKF__afdd__16a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__16a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '16A'})
  },
});
 const ovrs__EKF__afdd__20a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__20a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '20A'})
  },
});
 const ovrs__EKF__afdd__25a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__25a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '25A'})
  },
});
 const ovrs__EKF__afdd__32a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__32a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '32A'})
  },
});
 const ovrs__EKF__afdd__40a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__40a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '40A'})
  },
});
 const ovrs__EKF__afdd__50a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__50a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '50A'})
  },
});
 const ovrs__EKF__afdd__63a = defaultShape.extend({

  NAME: "ovrs__EKF__afdd__63a",

  init: function (attr) {
    const realWidth = 36.5;
    const realHeight = 80.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/EKF/afdd/icon.png?itok=0FCSIi2g",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},
    ];

    const setYb = [
      {value: 25.47, radius: 5},
      {value: 73.71, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.7 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x, y, text: '63A'})
  },
});
 const ovrs__istok__YZDP_1P_63A = defaultShape.extend({

  NAME: "ovrs__istok__YZDP_1P_63A",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 85.6;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/ovrs/istok/YZDP_1P_63A/icon.png?itok=HOE-8iRI",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.48, radius: 5 },
      { value: 74.52, radius: 5 },
    ];

    const setYb = [
      { value: 25.48, radius: 5 },
      { value: 74.52, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const power__mw__lrs__lrs350__LRS_350_12 = defaultShape.extend({

  NAME: "power__mw__lrs__lrs350__LRS_350_12",

  init: function (attr) {
    const realWidth = 115;
    const realHeight = 215;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/LRS/350/icon.png?itok=ZaHlCDn7",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 15.44, radius: 5 },
      { value: 24.30, radius: 5 },
      { value: 32.75, radius: 5 },
      { value: 41.61, radius: 5 },
      { value: 50.34, radius: 5 },
      { value: 59.33, radius: 5 },
      { value: 67.79, radius: 5 },
      { value: 76.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '350W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '12V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__lrs__lrs350__LRS_350_15 = defaultShape.extend({

  NAME: "power__mw__lrs__lrs350__LRS_350_15",

  init: function (attr) {
    const realWidth = 115;
    const realHeight = 215;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/LRS/350/icon.png?itok=ZaHlCDn7",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 15.44, radius: 5 },
      { value: 24.30, radius: 5 },
      { value: 32.75, radius: 5 },
      { value: 41.61, radius: 5 },
      { value: 50.34, radius: 5 },
      { value: 59.33, radius: 5 },
      { value: 67.79, radius: 5 },
      { value: 76.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '350W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '15V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__lrs__lrs350__LRS_350_24 = defaultShape.extend({

  NAME: "power__mw__lrs__lrs350__LRS_350_24",

  init: function (attr) {
    const realWidth = 115;
    const realHeight = 215;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/LRS/350/icon.png?itok=ZaHlCDn7",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 15.44, radius: 5 },
      { value: 24.30, radius: 5 },
      { value: 32.75, radius: 5 },
      { value: 41.61, radius: 5 },
      { value: 50.34, radius: 5 },
      { value: 59.33, radius: 5 },
      { value: 67.79, radius: 5 },
      { value: 76.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '350W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '24V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__lrs__lrs350__LRS_350_36 = defaultShape.extend({

  NAME: "power__mw__lrs__lrs350__LRS_350_36",

  init: function (attr) {
    const realWidth = 115;
    const realHeight = 215;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/LRS/350/icon.png?itok=ZaHlCDn7",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 15.44, radius: 5 },
      { value: 24.30, radius: 5 },
      { value: 32.75, radius: 5 },
      { value: 41.61, radius: 5 },
      { value: 50.34, radius: 5 },
      { value: 59.33, radius: 5 },
      { value: 67.79, radius: 5 },
      { value: 76.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '350W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '36V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__lrs__lrs350__LRS_350_48 = defaultShape.extend({

  NAME: "power__mw__lrs__lrs350__LRS_350_48",

  init: function (attr) {
    const realWidth = 115;
    const realHeight = 215;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/LRS/350/icon.png?itok=ZaHlCDn7",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 15.44, radius: 5 },
      { value: 24.30, radius: 5 },
      { value: 32.75, radius: 5 },
      { value: 41.61, radius: 5 },
      { value: 50.34, radius: 5 },
      { value: 59.33, radius: 5 },
      { value: 67.79, radius: 5 },
      { value: 76.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '350W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '48V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr75__NDR_75_12 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr75__NDR_75_12",

  init: function (attr) {
    const realWidth = 32;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/75/icon.png?itok=NRLpb2FB",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 27.52, radius: 5 },
      { value: 44.34, radius: 5 },
      { value: 63.30, radius: 5 },
      { value: 80.12, radius: 5 },
    ];

    const setYb = [
      { value: 33.64, radius: 5 },
      { value: 50.15, radius: 5 },
      { value: 66.36, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '75W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '12V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr75__ndr_75_24 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr75__ndr_75_24",

  init: function (attr) {
    const realWidth = 32;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/75/icon.png?itok=NRLpb2FB",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 27.52, radius: 5 },
      { value: 44.34, radius: 5 },
      { value: 63.30, radius: 5 },
      { value: 80.12, radius: 5 },
    ];

    const setYb = [
      { value: 33.64, radius: 5 },
      { value: 50.15, radius: 5 },
      { value: 66.36, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '75W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '24V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr75__ndr_75_48 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr75__ndr_75_48",

  init: function (attr) {
    const realWidth = 32;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/75/icon.png?itok=NRLpb2FB",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 27.52, radius: 5 },
      { value: 44.34, radius: 5 },
      { value: 63.30, radius: 5 },
      { value: 80.12, radius: 5 },
    ];

    const setYb = [
      { value: 33.64, radius: 5 },
      { value: 50.15, radius: 5 },
      { value: 66.36, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '75W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '48V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr120__NDR_120_12 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr120__NDR_120_12",

  init: function (attr) {
    const realWidth = 40;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/120/icon.png?itok=FTohiSXS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 31.94, radius: 5 },
      { value: 45.21, radius: 5 },
      { value: 60.69, radius: 5 },
      { value: 73.71, radius: 5 },
    ];

    const setYb = [
      { value: 36.61, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 63.14, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '120W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '12V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr120__ndr_120_24 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr120__ndr_120_24",

  init: function (attr) {
    const realWidth = 40;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/120/icon.png?itok=FTohiSXS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 31.94, radius: 5 },
      { value: 45.21, radius: 5 },
      { value: 60.69, radius: 5 },
      { value: 73.71, radius: 5 },
    ];

    const setYb = [
      { value: 36.61, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 63.14, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '120W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '24V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr120__ndr_120_48 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr120__ndr_120_48",

  init: function (attr) {
    const realWidth = 40;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/120/icon.png?itok=FTohiSXS",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 31.94, radius: 5 },
      { value: 45.21, radius: 5 },
      { value: 60.69, radius: 5 },
      { value: 73.71, radius: 5 },
    ];

    const setYb = [
      { value: 36.61, radius: 5 },
      { value: 50.12, radius: 5 },
      { value: 63.14, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '120W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '48V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr240__NDR_240_12 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr240__NDR_240_12",

  init: function (attr) {
    const realWidth = 63;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/240/icon.png?itok=bkXamWOJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 38.46, radius: 5 },
      { value: 46.62, radius: 5 },
      { value: 56.51, radius: 5 },
      { value: 64.99, radius: 5 },
    ];

    const setYb = [
      { value: 41.44, radius: 5 },
      { value: 49.76, radius: 5 },
      { value: 58.40, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '240W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '12V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr240__ndr_240_24 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr240__ndr_240_24",

  init: function (attr) {
    const realWidth = 63;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/240/icon.png?itok=bkXamWOJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 38.46, radius: 5 },
      { value: 46.62, radius: 5 },
      { value: 56.51, radius: 5 },
      { value: 64.99, radius: 5 },
    ];

    const setYb = [
      { value: 41.44, radius: 5 },
      { value: 49.76, radius: 5 },
      { value: 58.40, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '240W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '24V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr240__ndr_240_48 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr240__ndr_240_48",

  init: function (attr) {
    const realWidth = 63;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/240/icon.png?itok=bkXamWOJ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 38.46, radius: 5 },
      { value: 46.62, radius: 5 },
      { value: 56.51, radius: 5 },
      { value: 64.99, radius: 5 },
    ];

    const setYb = [
      { value: 41.44, radius: 5 },
      { value: 49.76, radius: 5 },
      { value: 58.40, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '240W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '48V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr480__NDR_480_12 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr480__NDR_480_12",

  init: function (attr) {
    const realWidth = 85.5;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/480/icon.png?itok=vrF8WeP_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 65.31, radius: 5 },
      { value: 71.46, radius: 5 },
      { value: 78.65, radius: 5 },
      { value: 84.92, radius: 5 },
    ];

    const setYb = [
      { value: 29.35, radius: 5 },
      { value: 35.73, radius: 5 },
      { value: 41.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '480W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '12V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr480__ndr_480_24 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr480__ndr_480_24",

  init: function (attr) {
    const realWidth = 85.5;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/480/icon.png?itok=vrF8WeP_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 65.31, radius: 5 },
      { value: 71.46, radius: 5 },
      { value: 78.65, radius: 5 },
      { value: 84.92, radius: 5 },
    ];

    const setYb = [
      { value: 29.35, radius: 5 },
      { value: 35.73, radius: 5 },
      { value: 41.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '480W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '24V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const power__mw__ndr__ndr480__ndr_480_48 = defaultShape.extend({

  NAME: "power__mw__ndr__ndr480__ndr_480_48",

  init: function (attr) {
    const realWidth = 85.5;
    const realHeight = 125.2;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/power/MW/NDR/480/icon.png?itok=vrF8WeP_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 65.31, radius: 5 },
      { value: 71.46, radius: 5 },
      { value: 78.65, radius: 5 },
      { value: 84.92, radius: 5 },
    ];

    const setYb = [
      { value: 29.35, radius: 5 },
      { value: 35.73, radius: 5 },
      { value: 41.88, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.70 * realHeight * Utils.sizeCoef;
    const y1 = 0.80 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '480W', fontColor: "rgba(255,255,255,1)", })
    Utils.createLabel(this, { x: 0, y: y1, text: '48V', fontColor: "rgba(255,255,255,1)", })
  },
});
 const rcd__DEKraft__YZO_03__p2__10a__10ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__10a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__10a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__10a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__10a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__10a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__10a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__10a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__16a__10ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__16a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__16a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__16a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__16a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__16a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__16a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__16a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__20a__10ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__20a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__20a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__20a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__20a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__20a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__20a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__20a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__25a__10ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__25a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__25a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__25a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__25a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__25a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__25a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__25a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__32a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__32a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__32a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__32a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__32a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__32a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__40a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__40a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__40a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__40a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__40a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__40a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__63a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__63a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__63a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__63a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__63a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__63a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__80a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__80a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__80a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__80a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__80a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__80a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__100a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__100a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__100a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__100a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p2__100a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p2__100a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_2/icon.png?itok=KUvK-7DN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })
  },
});
 const rcd__DEKraft__YZO_03__p4__10a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__10a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__10a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__10a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__10a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__10a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '10A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__16a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__16a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__16a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__16a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__16a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__16a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__25a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__25a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__25a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__25a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__25a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__25a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__32a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__32a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__32a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__32a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__32a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__32a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__40a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__40a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__40a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__40a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__40a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__40a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__63a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__63a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__63a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__63a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__63a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__63a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__80a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__80a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__80a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__80a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__80a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__80a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__100a__30ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__100a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__100a__100ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__100a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 8 })

  },
});
 const rcd__DEKraft__YZO_03__p4__100a__300ma = defaultShape.extend({

  NAME: "rcd__DEKraft__YZO_03__p4__100a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 78.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/DEKraft%20/%D0%A3%D0%97%D0%9E-03/pole_4/icon.png?itok=CoIYvmip",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2 + 5;
    const y = 0.42 * realHeight * Utils.sizeCoef;
    const y2 = 0.52 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 8 })

  },
});
 const rcd__ekf__averes__dv__p4__16a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__16a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__16a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__16a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__16a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__16a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__16a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__16a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__25a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__25a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__25a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__25a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__25a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__25a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__25a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__25a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__32a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__32a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__32a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__32a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__32a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__32a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__32a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__32a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__40a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__40a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__40a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__40a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__40a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__40a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__40a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__40a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__63a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__63a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__63a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__63a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__63a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__63a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__63a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__63a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__80a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__80a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__80a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__80a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__80a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__80a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__80a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__80a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__100a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__100a__10ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '10mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__100a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__100a__30ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '30mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__100a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__100a__100ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '100mA', fontSize: 10 })
  },
});
 const rcd__ekf__averes__dv__p4__100a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__averes__dv__p4__100a__300ma",

  init: function (attr) {
    const realWidth = 70.5;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/AVERES/DV/pole_4/icon.png?itok=VfSj2idI",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    const setYb = [
      { value: 11.10, radius: 5 },
      { value: 36.24, radius: 5 },
      { value: 61.66, radius: 5 },
      { value: 86.94, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.31 * (realWidth * Utils.sizeCoef);
    const y = 0.32 * realHeight * Utils.sizeCoef;
    const y2 = 0.4 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, { x, y: y2, text: '300mA', fontSize: 10 })
  },
});
 const rcd__ekf__proxima__vd_100__p2__16a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__16a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__16a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__16a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__16a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__16a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__16a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__16a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__25a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__25a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__25a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__25a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__25a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__25a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__25a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__25a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__32a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__32a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__32a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__32a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__32a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__32a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__32a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__32a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__40a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__40a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__40a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__40a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__40a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__40a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__40a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__40a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__50a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__50a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__50a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__50a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__50a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__50a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__50a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__50a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__63a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__63a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__63a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__63a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__63a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__63a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__63a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__63a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__80a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__80a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__80a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__80a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__80a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__80a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__80a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__80a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__100a__10ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__100a__10ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__100a__30ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__100a__30ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__100a__100ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__100a__100ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__ekf__proxima__vd_100__p2__100a__300ma = defaultShape.extend({

  NAME: "rcd__ekf__proxima__vd_100__p2__100a__300ma",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 81;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/EKF/PROXIMA/VD_100/pole_2/icon.png?itok=YtN-jS8K",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * (realWidth * Utils.sizeCoef );
    const y = 0.47 * realHeight * Utils.sizeCoef;
    const y2 = 0.57 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__16a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__16a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__16a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__16a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__16a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__20a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__20a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__20a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__20a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__20a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__25a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__25a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__25a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__25a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__25a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__32a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__32a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__32a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__32a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__32a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__40a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__40a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__40a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__40a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__40a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__63a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__63a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__63a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__63a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__63a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__80a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__80a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__80a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__80a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__80a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__100a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__100a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__100a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__100a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p2__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p2__100a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_2/icon.png?itok=a3C_wNcH",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__armat__p4__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__16a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__16a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__16a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__16a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__16a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__20a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__20a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__20a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__20a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__20a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__25a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__25a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__25a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__25a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__25a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__32a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__32a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__32a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__32a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__32a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__40a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__40a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__40a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__40a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__40a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__63a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__63a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__63a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__63a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__63a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__80a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__80a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__80a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__80a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__80a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__100a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__100a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__100a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__100a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__armat__p4__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__armat__p4__100a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/armat/pole_4/icon.png?itok=bVcHAzfi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    const y = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x, y: y2, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p2__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__16a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__16a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__16a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__16a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__16a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__20a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__20a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__20a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__20a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__20a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__25a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__25a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__25a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__25a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__25a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__32a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__32a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__32a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__32a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__32a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__40a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__40a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__40a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__40a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__40a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__63a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__63a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__63a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__63a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__63a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__80a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__80a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__80a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__80a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__80a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__100a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__100a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__100a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__100a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p2__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p2__100a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_2/icon.png?itok=QwNCPPr8",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__generica__p4__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__16a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__16a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__16a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__16a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__16a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__20a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__20a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__20a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__20a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__20a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__25a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__25a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__25a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__25a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__25a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__32a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__32a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__32a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__32a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__32a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__40a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__40a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__40a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__40a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__40a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__63a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__63a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__63a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__63a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__63a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__80a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__80a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__80a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__80a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__80a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__100a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__100a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__100a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__100a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__generica__p4__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__generica__p4__100a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/generica/pole_4/icon.png?itok=7Ed98szN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.76 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p2__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__16a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__16a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__16a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__16a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__16a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__20a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__20a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__20a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__20a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__20a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__25a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__25a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__25a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__25a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__25a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__32a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__32a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__32a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__32a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__32a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__40a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__40a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__40a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__40a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__40a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__63a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__63a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__63a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__63a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__63a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__80a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__80a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__80a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__80a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__80a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__100a__10ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__100a__30ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__100a__100ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__100a__300ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p2__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p2__100a__500ma",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_2/icon.png?itok=8_AkUEJP",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.675 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})
  },
});
 const rcd__iek__karat__p4__16a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__16a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__16a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__16a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__16a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__16a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__16a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__16a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__16a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__16a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '16A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__20a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__20a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__20a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__20a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__20a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__20a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__20a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__20a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__20a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__20a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '20A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__25a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__25a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__25a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__25a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__25a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__25a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__25a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__25a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__25a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__25a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '25A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__32a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__32a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__32a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__32a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__32a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__32a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__32a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__32a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__32a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__32a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '32A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__40a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__40a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__40a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__40a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__40a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__40a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__40a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__40a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__40a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__40a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '40A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__63a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__63a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__63a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__63a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__63a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__63a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__63a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__63a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__63a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__63a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '63A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__80a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__80a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__80a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__80a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__80a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__80a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__80a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__80a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__80a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__80a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '80A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__100a__10ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__100a__10ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '10mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__100a__30ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__100a__30ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '30mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__100a__100ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__100a__100ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '100mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__100a__300ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__100a__300ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '300mA', fontSize: 8})

  },
});
 const rcd__iek__karat__p4__100a__500ma = defaultShape.extend({

  NAME: "rcd__iek__karat__p4__100a__500ma",

  init: function (attr) {
    const din = 4;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/rcd/iek/karat/pole_4/icon.png?itok=yJkU507D",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    const setYb = [
      { value: 12.5, radius: 5 },
      { value: 37.5, radius: 5 },
      { value: 62.5, radius: 5 },
      { value: 87.5, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din-1)/2;
    const y = 0.51 * realHeight * Utils.sizeCoef;
    const x2 = (realWidth * Utils.sizeCoef / din) * -(din-3)/2;
    Utils.createLabel(this, { x, y, text: '100A' })
    Utils.createLabel(this, {x: x2, y, text: '500mA', fontSize: 8})

  },
});
 const relay__CNC__YC9VA = defaultShape.extend({

  NAME: "relay__CNC__YC9VA",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 82;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/CNC/YC9VA/icon.png?itok=hqQ8Vg8O",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 14.97, radius: 5 },
      { value: 49.72, radius: 5 },
      { value: 84.84, radius: 5 },
    ];
    
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const relay__DigiTop__PS_63A = defaultShape.extend({

  NAME: "relay__DigiTop__PS_63A",

  init: function (attr) {
    const realWidth = 122.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/PS_63A/icon.png?itok=Y4Qd39Yj",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 6.98, radius: 5 },
      { value: 35.66, radius: 5 },
      { value: 64.34, radius: 5 },
      { value: 93.02, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 35.66, radius: 5 },
      { value: 64.34, radius: 5 },
      { value: 93.02, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__DigiTop__VP__25a = defaultShape.extend({

  NAME: "relay__DigiTop__VP__25a",

  init: function (attr) {
    const realWidth = 55;
    const realHeight = 95;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/VP/icon.png?itok=vVZaPIqz",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.40, radius: 5 },
      { value: 49.91, radius: 5 },
      { value: 83.24, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.37 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'VP-25A', fontSize: 8 })
  },
});
 const relay__DigiTop__VP__32a = defaultShape.extend({

  NAME: "relay__DigiTop__VP__32a",

  init: function (attr) {
    const realWidth = 55;
    const realHeight = 95;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/VP/icon.png?itok=vVZaPIqz",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.40, radius: 5 },
      { value: 49.91, radius: 5 },
      { value: 83.24, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.37 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'VP-32A', fontSize: 8 })
  },
});
 const relay__DigiTop__VP__40a = defaultShape.extend({

  NAME: "relay__DigiTop__VP__40a",

  init: function (attr) {
    const realWidth = 55;
    const realHeight = 95;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/VP/icon.png?itok=vVZaPIqz",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.40, radius: 5 },
      { value: 49.91, radius: 5 },
      { value: 83.24, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.37 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'VP-40A', fontSize: 8 })
  },
});
 const relay__DigiTop__VP__50a = defaultShape.extend({

  NAME: "relay__DigiTop__VP__50a",

  init: function (attr) {
    const realWidth = 55;
    const realHeight = 95;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/VP/icon.png?itok=vVZaPIqz",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.40, radius: 5 },
      { value: 49.91, radius: 5 },
      { value: 83.24, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.37 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'VP-50A', fontSize: 8 })
  },
});
 const relay__DigiTop__VP__63a = defaultShape.extend({

  NAME: "relay__DigiTop__VP__63a",

  init: function (attr) {
    const realWidth = 55;
    const realHeight = 95;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/DigiTop/VP/icon.png?itok=vVZaPIqz",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.40, radius: 5 },
      { value: 49.91, radius: 5 },
      { value: 83.24, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x1 = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.37 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: x1, y, text: 'VP-63A', fontSize: 8 })
  },
});
 const relay__ekf__proxima__MRVA__MRVA_50A = defaultShape.extend({

  NAME: "relay__ekf__proxima__MRVA__MRVA_50A",

  init: function (attr) {
    const realWidth = 53.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/MRVA/MRVA_50A/icon.png?itok=kJ24QzP8",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 17.34, radius: 5 },
      { value: 83.03, radius: 5 },
    ];

    const setYb = [
      { value: 17.34, radius: 5 },
      { value: 83.03, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.73 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const relay__EKF__proxima_MRVA_63A = defaultShape.extend({

  NAME: "relay__EKF__proxima_MRVA_63A",

  init: function (attr) {
    const realWidth = 53.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/MRVA/MRVA_63A/icon.png?itok=Ha_qULA_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 17.34, radius: 5 },
      { value: 83.03, radius: 5 },
    ];

    const setYb = [
      { value: 17.34, radius: 5 },
      { value: 83.03, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.25 * realWidth * Utils.sizeCoef;
    const y = 0.73 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const relay__ekf__proxima__MRVA_3_63A = defaultShape.extend({

  NAME: "relay__ekf__proxima__MRVA_3_63A",

  init: function (attr) {
    const realWidth = 89.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/MRVA_3_63A/icon.png?itok=1p4Hx6C3",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.51, radius: 5 },
      { value: 37.63, radius: 5 },
      { value: 62.56, radius: 5 },
      { value: 87.58, radius: 5 },
    ];

    const setYb = [
      { value: 12.51, radius: 5 },
      { value: 37.63, radius: 5 },
      { value: 62.56, radius: 5 },
      { value: 87.58, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__ekf__proxima__RIO_1 = defaultShape.extend({

  NAME: "relay__ekf__proxima__RIO_1",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/RIO-1/icon.png?itok=YpoM5G8j",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'RIO-1', fontSize: 10 })
  },
});
 const relay__ekf__proxima__RKF_2S = defaultShape.extend({

  NAME: "relay__ekf__proxima__RKF_2S",

  init: function (attr) {
    const realWidth = 35;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/RKF_2S/icon.png?itok=fNlMm4xj",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.55, radius: 5 },
      { value: 39.27, radius: 5 },
      { value: 60.36, radius: 5 },
      { value: 81.45, radius: 5 },
    ];

    const setYb = [
      { value: 16.36, radius: 3 },
      { value: 29.82, radius: 3 },
      { value: 43.27, radius: 3 },
      { value: 57.09, radius: 3 },
      { value: 70.55, radius: 3 },
      { value: 83.27, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__ekf__proxima__TE_80 = defaultShape.extend({

  NAME: "relay__ekf__proxima__TE_80",

  init: function (attr) {
    const realWidth = 35;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/EKF/proxima/TE_80/icon.png?itok=QVM7BG9d",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.5, radius: 5 },
    ];

    const setYb = [
      { value: 18.28, radius: 5 },
      { value: 39.34, radius: 5 },
      { value: 60.66, radius: 5 },
      { value: 81.72, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__ekm__rkch__m01 = defaultShape.extend({

  NAME: "relay__ekm__rkch__m01",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 93;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/ekm/rkch/m01/icon.png?itok=w5edDPDy",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 32.46, radius: 3 },
      { value: 68.59, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 9 / 100;

    const setYb1 = [
      { value: 27.23, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 73.82, radius: 3 },
    ];
    const yb1x = realWidth * Utils.sizeCoef * 79 / 100;

    const setYb2 = [
      { value: 27.23, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 73.82, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 90 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);

    Utils.createSetOfPortsCustomX(this, setYb1, yb1x, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);
  },
});
 const relay__ekm__rkch__m02 = defaultShape.extend({

  NAME: "relay__ekm__rkch__m02",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 93;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/ekm/rkch/m02/icon.png?itok=KQVXnrML",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 32.46, radius: 3 },
      { value: 68.59, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 9 / 100;


    const setYt2 = [
      { value: 27.23, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 73.82, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 21 / 100;


    const setYb = [
      { value: 27.23, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 73.82, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 79 / 100;

    const setYb2 = [
      { value: 27.23, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 73.82, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 90 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);
  },
});
 const relay__fotek__SSR_40DA = defaultShape.extend({

  NAME: "relay__fotek__SSR_40DA",

  init: function (attr) {
    const din = 1;
    const realWidth = 45;
    const realHeight = 62;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/fotek/SSR-40DA/icon.png?itok=HW5eMWR9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 20.17, radius: 5 },
      { value: 80.48, radius: 5 },
    ];

    const setYb = [
      { value: 20.17, radius: 5 },
      { value: 80.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.6 * realHeight * Utils.sizeCoef;

    Utils.createLabel(this, { x: 0, y, text: 'SSR 60 DA', fontSize: 10 })
  },
});
 const relay__f_f__cp_723 = defaultShape.extend({

  NAME: "relay__f_f__cp_723",

  init: function (attr) {
    const realWidth = 105;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/CP_723/icon.png?itok=u3V1h-wy",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 88.02, radius: 5 },
    ];

    const setYb = [
      { value: 10.65, radius: 5 },
      { value: 20.28, radius: 5 },
      { value: 30.23, radius: 5 },
      { value: 40.09, radius: 5 },
      { value: 49.96, radius: 5 },
      { value: 59.91, radius: 5 },
      { value: 69.62, radius: 5 },
      { value: 79.64, radius: 5 },
      { value: 89.51, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__f_f__impuls__BIS_411 = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_411",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_411/icon.png?itok=4f8Kplix",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '411', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_411i = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_411i",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_411i/icon.png?itok=qm3rmMAZ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '411i', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_412 = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_412",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_412/icon.png?itok=Q6Vb0nOT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '412', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_412i = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_412i",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_412i/icon.png?itok=b1y_qr_l",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '412i', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_412_T = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_412_T",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_412_%D0%A2/icon.png?itok=_DY5z2mw",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '412-T', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_413 = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_413",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_413/icon.png?itok=Xo1ZzkZK",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);

    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '413', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_413i = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_413i",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_413i/icon.png?itok=KLEwpwvN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '413i', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_414 = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_414",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_414/icon.png?itok=qFTr3sMQ",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '414', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_414i = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_414i",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_414i/icon.png?itok=8UpuNrTM",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '414i', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_419 = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_419",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_419/icon.png?itok=RwAlAHyE",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.63 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '419', fontSize: 10 })
  },
});
 const relay__f_f__impuls__BIS_419i = defaultShape.extend({

  NAME: "relay__f_f__impuls__BIS_419i",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/impuls/BIS_419i/icon.png?itok=xUPktqY3",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 8 / 100;

    const setYt2 = [
      { value: 27.11, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 20 / 100;

    const setYb = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const ybx = realWidth * Utils.sizeCoef * 80 / 100;

    const setYb2 = [
      { value: 26.67, radius: 3 },
      { value: 49.78, radius: 3 },
      { value: 73.78, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 92 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb, ybx, realWidth, realHeight, 2);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.63 * realHeight * Utils.sizeCoef;
    const y2 = 0.78 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'BIS', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '419i', fontSize: 10 })
  },
});
 const relay__f_f__pf__pf_431 = defaultShape.extend({

  NAME: "relay__f_f__pf__pf_431",

  init: function (attr) {
    const realWidth = 52;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/PF/PF-431/icon.png?itok=XU_-lIPO",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 11.09, radius: 5},
      {value: 26.88, radius: 5},
      {value: 42.29, radius: 5},
      {value: 57.71, radius: 5},
      {value: 73.12, radius: 5},
      {value: 88.53, radius: 5},
    ];

    const setYb = [
      {value: 11.09, radius: 5},
      {value: 26.88, radius: 5},
      {value: 42.29, radius: 5},
      {value: 57.71, radius: 5},
      {value: 73.12, radius: 5},
      {value: 88.53, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__f_f__pf__pf_441 = defaultShape.extend({

  NAME: "relay__f_f__pf__pf_441",

  init: function (attr) {
    const realWidth = 52;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/PF/PF-441/icon.png?itok=0XVESGyi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 11.09, radius: 5},
      {value: 26.88, radius: 5},
      {value: 42.29, radius: 5},
      {value: 57.71, radius: 5},
      {value: 73.12, radius: 5},
      {value: 88.53, radius: 5},
    ];

    const setYb = [
      {value: 11.09, radius: 5},
      {value: 26.88, radius: 5},
      {value: 42.29, radius: 5},
      {value: 57.71, radius: 5},
      {value: 73.12, radius: 5},
      {value: 88.53, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__f_f__pf__pf_451_1 = defaultShape.extend({

  NAME: "relay__f_f__pf__pf_451_1",

  init: function (attr) {
    const realWidth = 105;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/PF/PF-451-1/icon.png?itok=rQPRTIl8",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 82.40, radius: 5 },
      { value: 89.78, radius: 5 },
    ];

    const setYb = [
      { value: 8.80, radius: 5 },
      { value: 19.02, radius: 5 },
      { value: 29.23, radius: 5 },
      { value: 39.55, radius: 5 },
      { value: 49.86, radius: 5 },
      { value: 60.17, radius: 5 },
      { value: 70.48, radius: 5 },
      { value: 80.79, radius: 5 },
      { value: 91.11, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__f_f__pf__pf_452 = defaultShape.extend({

  NAME: "relay__f_f__pf__pf_452",

  init: function (attr) {
    const realWidth = 87;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/PF/PF-452/icon.png?itok=JmUD_qpI",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.15, radius: 5 },
      { value: 18.93, radius: 5 },
      { value: 27.82, radius: 5 },
      { value: 36.60, radius: 5 },
      { value: 45.61, radius: 5 },
      { value: 54.39, radius: 5 },
      { value: 63.06, radius: 5 },
      { value: 71.95, radius: 5 },
      { value: 80.84, radius: 5 },
      { value: 89.74, radius: 5 },
    ];

    const setYb = [
      { value: 7.98, radius: 5 },
      { value: 14.03, radius: 5 },
      { value: 26.11, radius: 5 },
      { value: 32.16, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__f_f__PZ_832 = defaultShape.extend({

  NAME: "relay__f_f__PZ_832",

  init: function (attr) {
    const realWidth = 90;
    const realHeight = 87;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/PZ-832/icon.png?itok=2vAdbb6J",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 7.88, radius: 5 },
      { value: 14.27, radius: 5 },
      { value: 20.32, radius: 5 },
      { value: 26.03, radius: 5 },
      { value: 32.19, radius: 5 },
      { value: 38.01, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 50.11, radius: 5 },
      { value: 56.05, radius: 5 },
      { value: 62.10, radius: 5 },
      { value: 68.26, radius: 5 },
      { value: 74.09, radius: 5 },
      { value: 86.19, radius: 5 },
      { value: 92.12, radius: 5 },
    ];

    const setYb = [
      { value: 36.76, radius: 5 },
      { value: 45.89, radius: 5 },
      { value: 54.68, radius: 5 },
      { value: 72.26, radius: 5 },
      { value: 81.16, radius: 5 },
      { value: 89.84, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    const x = 0.2 * realWidth * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'PZ-832', fontSize: 12 })
  },
});
 const relay__f_f__cp_721_1 = defaultShape.extend({

  NAME: "relay__f_f__cp_721_1",

  init: function (attr) {
    const realWidth = 35;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/F_F/%D0%A1%D0%A0-721-1/icon.png?itok=IxisUObm",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 21.77, radius: 5},
      {value: 50.13, radius: 5},
      {value: 77.72, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__luna__LUNA_122_top3_RC = defaultShape.extend({

  NAME: "relay__luna__LUNA_122_top3_RC",

  init: function (attr) {
    const realWidth = 35;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/luna/LUNA_122_top3_RC/icon.png?itok=v-73jQlL",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 12.81, radius: 3},
      {value: 33.15, radius: 3},
      {value: 53.48, radius: 3},
      {value: 73.82, radius: 3},
    ];

    const setYb = [
      {value: 26.74, radius: 3},
      {value: 47.27, radius: 3},
      {value: 67.61, radius: 3},
      {value: 87.95, radius: 3},

    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const height = realHeight * Utils.sizeCoef;
    const width = realWidth * Utils.sizeCoef;

    Utils.createPort(this, {locatorAttr: {x: width * 20 / 100, y: height * 33 / 100}, direction: 0, width: 3, height: 3});
    Utils.createPort(this, {locatorAttr: {x: width * 20 / 100 , y: height* 12 / 100}, direction: 0, width: 3, height: 3});

  },
});
 const relay__meandr__RIO_1 = defaultShape.extend({

  NAME: "relay__meandr__RIO_1",

  init: function (attr) {
    const din = 1;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/meandr/RIO-1/icon.png?itok=37kLN46A",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 23.39, radius: 3},
      {value: 50.00, radius: 3},
      {value: 76.61, radius: 3},
    ];

    const setYb = [
      {value: 23.39, radius: 3},
      {value: 50.00, radius: 3},
      {value: 76.61, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.5 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'РИО-1', fontSize: 10 })
  },
});
 const relay__meandr__RIO_1M = defaultShape.extend({

  NAME: "relay__meandr__RIO_1M",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/meandr/RIO-1M/icon.png?itok=iNUb7yaC",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 28.37, radius: 5 },
      { value: 70.92, radius: 5 },
    ];
    const ytx = realWidth * Utils.sizeCoef * 9 / 100;

    const setYt2 = [
      { value: 28.37, radius: 5 },
      { value: 70.92, radius: 5 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 21 / 100;

    const setYb2 = [
      { value: 28.37, radius: 5 },
      { value: 70.92, radius: 5 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 91 / 100;

    Utils.createSetOfPortsCustomX(this, setYt, ytx, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


    const y1 = 0.5 * realHeight * Utils.sizeCoef;
    const y2 = 0.6 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y: y1, text: 'РИО', fontSize: 10 })
    Utils.createLabel(this, { x: 0, y: y2, text: '1M', fontSize: 10 })
  },
});
 const relay__meandr__RIY_1M = defaultShape.extend({

  NAME: "relay__meandr__RIY_1M",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/meandr/RIY-1M%20/icon.png?itok=zFxma3sd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 15.90, radius: 5},
      {value: 29.65, radius: 5},
      {value: 43.40, radius: 5},
      {value: 56.87, radius: 5},
      {value: 70.08, radius: 5},
      {value: 83.83, radius: 5},
    ];

    const setYb = [
      {value: 15.90, radius: 5},
      {value: 29.65, radius: 5},
      {value: 43.40, radius: 5},
      {value: 56.87, radius: 5},
      {value: 70.08, radius: 5},
      {value: 83.83, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.62 * realHeight * Utils.sizeCoef;

    Utils.createLabel(this, { x: 0, y, text: 'РИУ-1M', fontSize: 10 })
  },
});
 const relay__novvatek__pef_319 = defaultShape.extend({

  NAME: "relay__novvatek__pef_319",

  init: function (attr) {
    const realWidth = 157;
    const realHeight = 90.8;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/novatek/PEF-319/icon.png?itok=ANZkCK-E",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 7.63, radius: 5 },
      { value: 13.50, radius: 5 },
      { value: 19.37, radius: 5 },
      { value: 25.87, radius: 5 },
      { value: 31.80, radius: 5 },
      { value: 37.54, radius: 5 },
      { value: 44.10, radius: 5 },
      { value: 49.97, radius: 5 },
      { value: 55.84, radius: 5 },
      { value: 62.40, radius: 5 },
      { value: 68.20, radius: 5 },
      { value: 74.07, radius: 5 },
      { value: 80.57, radius: 5 },
      { value: 86.44, radius: 5 },
      { value: 92.30, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const relay__novvatek__pef_320 = defaultShape.extend({

  NAME: "relay__novvatek__pef_320",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/novatek/PEF-320/icon.png?itok=lLXD0bBZ",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 16.17, radius: 5 },
      { value: 39.08, radius: 5 },
      { value: 61.73, radius: 5 },
      { value: 84.37, radius: 5 },
    ];

    const setYb = [
      { value: 16.17, radius: 5 },
      { value: 39.08, radius: 5 },
      { value: 61.73, radius: 5 },
      { value: 84.37, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const relay__novvatek__rev_225 = defaultShape.extend({

  NAME: "relay__novvatek__rev_225",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/novatek/REV-225/icon.png?itok=nlcEiYNh",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.65, radius: 5 },
    ];

    const setYb = [
      { value: 18.65, radius: 5 },
      { value: 39.19, radius: 5 },
      { value: 60.00, radius: 5 },
      { value: 80.54, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const relay__novvatek__ph_260t = defaultShape.extend({

  NAME: "relay__novvatek__ph_260t",

  init: function (attr) {
    const realWidth = 52;
    const realHeight = 93;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/novatek/%D0%A0%D0%9D-260%D0%A2/icon.png?itok=OqpLtoYb",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 16.48, radius: 5 },
      { value: 50.00, radius: 5 },
      { value: 83.52, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const relay__pltek__plabp1_80_2 = defaultShape.extend({

  NAME: "relay__pltek__plabp1_80_2",

  init: function (attr) {
    const realWidth = 107.6;
    const realHeight = 100;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/PLTEK/PLABP1-80.2/icon.png?itok=0Fq-MSHw",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 6.48, radius: 3 },
      { value: 9.99, radius: 3 },
      { value: 13.60, radius: 3 },
      { value: 17.30, radius: 3 },
      { value: 20.81, radius: 3 },
      { value: 74.84, radius: 5 },
      { value: 90.75, radius: 5 },
    ];

    const setYb = [
      { value: 6.48, radius: 3 },
      { value: 9.99, radius: 3 },
      { value: 13.60, radius: 3 },
      { value: 17.30, radius: 3 },
      { value: 20.81, radius: 3 },
      { value: 74.84, radius: 5 },
      { value: 90.75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const setYb2 = [
      { value: 74.83, radius: 5 },
      { value: 90.66, radius: 5 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 79.06 / 100;

    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);
  },
});
 const relay__protective__TOVPD3_63VA = defaultShape.extend({

  NAME: "relay__protective__TOVPD3_63VA",

  init: function (attr) {
    const realWidth = 91;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/protective/TOVPD3_63VA/icon.png?itok=G105-Q9-",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 12.42, radius: 5},
      {value: 37.58, radius: 5},
      {value: 62.42, radius: 5},
      {value: 87.58, radius: 5},
    ];

    const setYb = [
      {value: 12.42, radius: 5},
      {value: 37.58, radius: 5},
      {value: 62.42, radius: 5},
      {value: 87.58, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const relay__rbuz__d2__40a = defaultShape.extend({

  NAME: "relay__rbuz__d2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2/icon.png?itok=03wtGg6C",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 10 })
  },
});
 const relay__rbuz__d2__50a = defaultShape.extend({

  NAME: "relay__rbuz__d2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2/icon.png?itok=03wtGg6C",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50А', fontSize: 10 })
  },
});
 const relay__rbuz__d2__63a = defaultShape.extend({

  NAME: "relay__rbuz__d2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2/icon.png?itok=03wtGg6C",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 10 })
  },
});
 const relay__rbuz__d2_red__40a = defaultShape.extend({

  NAME: "relay__rbuz__d2_red__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2_red/icon.png?itok=5a-nWFta",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 10 })
  },
});
 const relay__rbuz__d2_red__50a = defaultShape.extend({

  NAME: "relay__rbuz__d2_red__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2_red/icon.png?itok=5a-nWFta",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50А', fontSize: 10 })
  },
});
 const relay__rbuz__d2_red__63a = defaultShape.extend({

  NAME: "relay__rbuz__d2_red__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D2_red/icon.png?itok=5a-nWFta",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 10 })
  },
});
 const relay__rbuz__d6_red__40a = defaultShape.extend({

  NAME: "relay__rbuz__d6_red__40a",

  init: function (attr) {
    const din = 6;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D6_red/icon.png?itok=G0LzbApS",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 7.84, radius: 5 },
      { value: 24.84, radius: 5 },
      { value: 57.89, radius: 5 },
      { value: 91.22, radius: 5 },
    ];

    const setYb = [
      { value: 7.84, radius: 5 },
      { value: 24.84, radius: 5 },
      { value: 57.89, radius: 5 },
      { value: 91.22, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.3 * realWidth * Utils.sizeCoef;
    const y = 0.73 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 10 })
  },
});
 const relay__rbuz__d6_red__63a = defaultShape.extend({

  NAME: "relay__rbuz__d6_red__63a",

  init: function (attr) {
    const din = 6;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/D6_red/icon.png?itok=G0LzbApS",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 7.84, radius: 5 },
      { value: 24.84, radius: 5 },
      { value: 57.89, radius: 5 },
      { value: 91.22, radius: 5 },
    ];

    const setYb = [
      { value: 7.84, radius: 5 },
      { value: 24.84, radius: 5 },
      { value: 57.89, radius: 5 },
      { value: 91.22, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.3 * realWidth * Utils.sizeCoef;
    const y = 0.73 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 10 })
  },
});
 const relay__rbuz__mf__25a = defaultShape.extend({

  NAME: "relay__rbuz__mf__25a",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/MF/icon.png?itok=RZoY-sg9",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.85, radius: 5},
      {value: 52.18, radius: 5},
      {value: 86.50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * realWidth * Utils.sizeCoef;
    const y = 0.15 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25А', fontSize: 14 })
  },
});
 const relay__rbuz__mf__32a = defaultShape.extend({

  NAME: "relay__rbuz__mf__32a",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/MF/icon.png?itok=RZoY-sg9",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.85, radius: 5},
      {value: 52.18, radius: 5},
      {value: 86.50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * realWidth * Utils.sizeCoef;
    const y = 0.15 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32А', fontSize: 14 })
  },
});
 const relay__rbuz__mf__40a = defaultShape.extend({

  NAME: "relay__rbuz__mf__40a",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/MF/icon.png?itok=RZoY-sg9",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.85, radius: 5},
      {value: 52.18, radius: 5},
      {value: 86.50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * realWidth * Utils.sizeCoef;
    const y = 0.15 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 14 })
  },
});
 const relay__rbuz__mf__50a = defaultShape.extend({

  NAME: "relay__rbuz__mf__50a",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/MF/icon.png?itok=RZoY-sg9",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.85, radius: 5},
      {value: 52.18, radius: 5},
      {value: 86.50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * realWidth * Utils.sizeCoef;
    const y = 0.15 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50А', fontSize: 14 })
  },
});
 const relay__rbuz__mf__63a = defaultShape.extend({

  NAME: "relay__rbuz__mf__63a",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/RBUZ/MF/icon.png?itok=RZoY-sg9",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      {value: 18.85, radius: 5},
      {value: 52.18, radius: 5},
      {value: 86.50, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.2 * realWidth * Utils.sizeCoef;
    const y = 0.15 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 14 })
  },
});
 const relay__welrok__d2__40a = defaultShape.extend({

  NAME: "relay__welrok__d2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2/icon.png?itok=-MKgA-0a",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 8 })
  },
});
 const relay__welrok__d2__50a = defaultShape.extend({

  NAME: "relay__welrok__d2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2/icon.png?itok=-MKgA-0a",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50А', fontSize: 8 })
  },
});
 const relay__welrok__d2__63a = defaultShape.extend({

  NAME: "relay__welrok__d2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2/icon.png?itok=-MKgA-0a",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 8 })
  },
});
 const relay__welrok__d2_red__40a = defaultShape.extend({

  NAME: "relay__welrok__d2_red__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2_red/icon.png?itok=k7g-C8ou",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40А', fontSize: 8 })
  },
});
 const relay__welrok__d2_red__50a = defaultShape.extend({

  NAME: "relay__welrok__d2_red__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2_red/icon.png?itok=k7g-C8ou",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50А', fontSize: 8 })
  },
});
 const relay__welrok__d2_red__63a = defaultShape.extend({

  NAME: "relay__welrok__d2_red__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/relay/Welrok/D2_red/icon.png?itok=k7g-C8ou",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = 0.2 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63А', fontSize: 8 })
  },
});
 const smartHome__loxone__1_Wire_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__1_Wire_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/1_Wire_Extension/icon.png?itok=TxWnSOML",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__24V_Power_Supply_0_4 = defaultShape.extend({

  NAME: "smartHome__loxone__24V_Power_Supply_0_4",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/24V_Power_Supply_0_4/icon.png?itok=A_j2k6ji",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 48.20, radius: 3 },
      { value: 79.14, radius: 3 },
    ];

    const setYb = [
      { value: 35.25, radius: 3 },
      { value: 66.19, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__24V_Power_Supply_1_25 = defaultShape.extend({

  NAME: "smartHome__loxone__24V_Power_Supply_1_25",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/24V_Power_Supply_1_25/icon.png?itok=C2BHF9K0",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 11.92, radius: 3},
      {value: 28.46, radius: 3},
    ];

    const setYb = [
      {value: 11.92, radius: 3},
      {value: 28.46, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Air_Base_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Air_Base_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Air_Base_Extension/icon.png?itok=XxESv5hd",
      realWidth,
      realHeight,
    }, attr));


    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__AI_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__AI_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/AI_Extension/icon.png?itok=tQhuQx3Q",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 20.00, radius: 3},
      {value: 27.12, radius: 3},
      {value: 34.52, radius: 3},
      {value: 41.64, radius: 3},
      {value: 58.63, radius: 3},
      {value: 65.75, radius: 3},
      {value: 72.88, radius: 3},
      {value: 80.27, radius: 3},
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__AO_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__AO_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/AO_Extension/icon.png?itok=5H3MqmF0",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 39.45, radius: 3 },
      { value: 46.58, radius: 3 },
      { value: 53.70, radius: 3 },
      { value: 61.10, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__DALI_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__DALI_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/DALI_Extension/icon.png?itok=svwHy0TL",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 41.37, radius: 3},
      {value: 58.90, radius: 3},
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Dimmer_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Dimmer_Extension",

  init: function (attr) {
    const realWidth = 157.7;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Dimmer_Extension/icon.png?itok=1oe9JQyP",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.31, radius: 5 },
      { value: 13.58, radius: 5 },
      { value: 16.91, radius: 5 },
      { value: 20.30, radius: 5 },
      { value: 23.51, radius: 5 },
      { value: 27.22, radius: 5 },
      { value: 30.55, radius: 5 },
      { value: 33.63, radius: 5 },
      { value: 36.96, radius: 5 },
      { value: 39.97, radius: 5 },
    ];

    const setYb = [
      { value: 10.56, radius: 5 },
      { value: 13.70, radius: 5 },
      { value: 16.97, radius: 5 },
      { value: 19.92, radius: 5 },
      { value: 23.00, radius: 5 },
      { value: 26.15, radius: 5 },
      { value: 29.35, radius: 5 },
      { value: 32.37, radius: 5 },
      { value: 35.58, radius: 5 },
      { value: 38.53, radius: 5 },
      { value: 41.86, radius: 5 },
      { value: 44.81, radius: 5 },
      { value: 47.96, radius: 5 },
      { value: 51.16, radius: 5 },
      { value: 54.18, radius: 5 },
      { value: 57.39, radius: 5 },
      { value: 67.69, radius: 5 },
      { value: 70.77, radius: 5 },
      { value: 73.79, radius: 5 },
      { value: 77.00, radius: 5 },
      { value: 80.26, radius: 5 },
      { value: 83.22, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__DI_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__DI_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/DI_Extension/icon.png?itok=UDO_LsfU",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 17, radius: 3 },
      { value: 24.27, radius: 3 },
      { value: 31.54, radius: 3 },
      { value: 38.81, radius: 3 },
      { value: 46.08, radius: 3 },
      { value: 53.35, radius: 3 },
      { value: 60.62, radius: 3 },
      { value: 67.89, radius: 3 },
      { value: 75.16, radius: 3 },
      { value: 82.43, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__DMX_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__DMX_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/DMX_Extension/icon.png?itok=8Da0TcrE",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__EnOcean_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__EnOcean_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/EnOcean_Extension/icon.png?itok=vAmtrJ3u",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 20.27, radius: 3 },
      { value: 35.07, radius: 3 },
      { value: 65.48, radius: 3 },
      { value: 80.00, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Froling_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Froling_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Froling_Extension/icon.png?itok=ME7K2lXW",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Internorm_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Internorm_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Internorm_Extension/icon.png?itok=5Wtxi2Rg",
      realWidth,
      realHeight,
    }, attr));

    const setYb = [
      { value: 20.27, radius: 3 },
      { value: 35.07, radius: 3 },
      { value: 65.48, radius: 3 },
      { value: 80.00, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__KNX_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__KNX_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/KNX_Extension/icon.png?itok=Wq-MUR_O",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 64.66, radius: 3 },
      { value: 80.27, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Miniserver = defaultShape.extend({

  NAME: "smartHome__loxone__Miniserver",

  init: function (attr) {
    const realWidth = 158;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Miniserver/icon.png?itok=s8i-QlJX",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 41.17, radius: 3 },
      { value: 42.947, radius: 3 },
      { value: 44.724, radius: 3 },
      { value: 46.501, radius: 3 },
      { value: 48.278, radius: 3 },
      { value: 50.055, radius: 3 },
      { value: 51.832, radius: 3 },
      { value: 53.609, radius: 3 },

      { value: 22.38, radius: 3 },
      { value: 24.05, radius: 3 },
      { value: 25.72, radius: 3 },
      { value: 27.39, radius: 3 },
    ];

    const setYb = [
      { value: 71.59, radius: 3 },
      { value: 74.98, radius: 3 },
      { value: 80.52, radius: 3 },
      { value: 84.29, radius: 3 },
      { value: 89.69, radius: 3 },
      { value: 93.59, radius: 3 },

      { value: 36.96, radius: 3 },
      { value: 40.156, radius: 3 },
      { value: 43.352, radius: 3 },
      { value: 46.548, radius: 3 },
      { value: 49.744, radius: 3 },
      { value: 52.94, radius: 3 },
      { value: 56.136, radius: 3 },
      { value: 59.33, radius: 3 },

      { value: 9.87, radius: 3 },
      { value: 13.093, radius: 3 },
      { value: 16.316, radius: 3 },
      { value: 19.539, radius: 3 },
      { value: 22.762, radius: 3 },
      { value: 25.985, radius: 3 },
      { value: 29.208, radius: 3 },
      { value: 32.43, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Miniserver_Compact = defaultShape.extend({

  NAME: "smartHome__loxone__Miniserver_Compact",

  init: function (attr) {
    const realWidth = 106.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Miniserver_Compact/icon.png?itok=l8p-xZnD",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 36.16, radius: 3 },
      { value: 38.86, radius: 3 },
      { value: 41.56, radius: 3 },
      { value: 44.27, radius: 3 },
    ];

    const setYb = [
      { value: 37.47, radius: 3 },
      { value: 42.87, radius: 3 },
      { value: 57.69, radius: 3 },
      { value: 63.09, radius: 3 },
      { value: 71.39, radius: 3 },
      { value: 76.79, radius: 3 },
      { value: 84.81, radius: 3 },
      { value: 90.49, radius: 3 },

      { value: 9.88, radius: 3 },
      { value: 14.79, radius: 3 },
      { value: 19.70, radius: 3 },
      { value: 24.60, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Modbus_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Modbus_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Modbus_Extension/icon.png?itok=3HLh7Qro",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Multi_Extension_Air = defaultShape.extend({

  NAME: "smartHome__loxone__Multi_Extension_Air",

  init: function (attr) {
    const realWidth = 158;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Multi_Extension_Air/icon.png?itok=lMP5w9io",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 64.05, radius: 3 },
      { value: 67.39, radius: 3 },
      { value: 70.73, radius: 3 },
      { value: 74.07, radius: 3 },
      { value: 77.41, radius: 3 },
      { value: 80.77, radius: 3 },

      { value: 45.25, radius: 3 },
      { value: 48.59, radius: 3 },
      { value: 51.93, radius: 3 },
      { value: 55.27, radius: 3 },
      { value: 58.61, radius: 3 },
      { value: 61.95, radius: 3 },

      { value: 32.68, radius: 3 },
      { value: 36.27, radius: 3 },
      { value: 39.79, radius: 3 },

      { value: 10.56, radius: 3 },
      { value: 13.86, radius: 3 },
      { value: 17.16, radius: 3 },
      { value: 20.46, radius: 3 },
      { value: 23.76, radius: 3 },
      { value: 27.06, radius: 3 },
    ];

    const setYb = [
      { value: 10.18, radius: 3 },
      { value: 13.89, radius: 3 },
      { value: 18.29, radius: 3 },
      { value: 21.94, radius: 3 },
      { value: 26.40, radius: 3 },
      { value: 30.11, radius: 3 },
      { value: 34.44, radius: 3 },
      { value: 38.15, radius: 3 },
      { value: 42.55, radius: 3 },
      { value: 46.26, radius: 3 },
      { value: 50.66, radius: 3 },
      { value: 54.37, radius: 3 },
      { value: 58.77, radius: 3 },
      { value: 62.54, radius: 3 },
      { value: 66.94, radius: 3 },
      { value: 70.65, radius: 3 },
      { value: 79.76, radius: 3 },
      { value: 83.53, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Relay_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Relay_Extension",

  init: function (attr) {
    const realWidth = 155;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Relay_Extension/icon.png?itok=RjNdpRNB",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.12, radius: 3 },
      { value: 13.76, radius: 3 },
      { value: 19.61, radius: 3 },
      { value: 23.38, radius: 3 },
      { value: 29.16, radius: 3 },
      { value: 32.87, radius: 3 },
      { value: 38.59, radius: 3 },
      { value: 42.24, radius: 3 },
      { value: 48.15, radius: 3 },
      { value: 51.92, radius: 3 },
      { value: 57.64, radius: 3 },
      { value: 61.28, radius: 3 },
      { value: 67.19, radius: 3 },
      { value: 70.96, radius: 3 },
      { value: 76.81, radius: 3 },
      { value: 80.52, radius: 3 },
    ];

    const setYb = [
      { value: 10.12, radius: 3 },
      { value: 13.89, radius: 3 },
      { value: 19.74, radius: 3 },
      { value: 23.38, radius: 3 },
      { value: 29.23, radius: 3 },
      { value: 32.81, radius: 3 },
      { value: 38.72, radius: 3 },
      { value: 42.30, radius: 3 },
      { value: 48.21, radius: 3 },
      { value: 51.85, radius: 3 },
      { value: 57.76, radius: 3 },
      { value: 61.35, radius: 3 },
      { value: 80.58, radius: 3 },
      { value: 84.35, radius: 3 },
      { value: 89.63, radius: 3 },
      { value: 93.46, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__RS232_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__RS232_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/RS232_Extension/icon.png?itok=Dk9mDQBK",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__RS485_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__RS485_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/RS485_Extension/icon.png?itok=RPYhcSBK",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Schueco_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Schueco_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Schueco_Extension/icon.png?itok=vl7xx9q_",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 43.01, radius: 3 },
      { value: 50.41, radius: 3 },
      { value: 57.53, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__loxone__Tree_Extension = defaultShape.extend({

  NAME: "smartHome__loxone__Tree_Extension",

  init: function (attr) {
    const realWidth = 35.4;
    const realHeight = 88;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Loxone/Tree_Extension/icon.png?itok=HW6j8qIw",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    const setYb = [
      { value: 23.56, radius: 3 },
      { value: 39.73, radius: 3 },
      { value: 63.56, radius: 3 },
      { value: 79.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__oven__PLK_150 = defaultShape.extend({

  NAME: "smartHome__oven__PLK_150",

  init: function (attr) {
    const realWidth = 105;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/oven/PLK_150/icon.png?itok=0QfC4poa",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 9.85, radius: 3 },
      { value: 17.05, radius: 3 },
      { value: 24.12, radius: 3 },
      { value: 31.31, radius: 3 },
      { value: 38.51, radius: 3 },
      { value: 45.71, radius: 3 },
      { value: 52.90, radius: 3 },
      { value: 59.97, radius: 3 },
      { value: 66.92, radius: 3 },
      { value: 74.24, radius: 3 },
      { value: 81.31, radius: 3 },
      { value: 88.51, radius: 3 },
    ];

    const setYb = [
      { value: 9.85, radius: 3 },
      { value: 17.05, radius: 3 },
      { value: 24.12, radius: 3 },
      { value: 31.31, radius: 3 },
      { value: 38.51, radius: 3 },
      { value: 45.71, radius: 3 },
      { value: 52.90, radius: 3 },
      { value: 59.97, radius: 3 },
      { value: 66.92, radius: 3 },
      { value: 74.24, radius: 3 },
      { value: 81.31, radius: 3 },
      { value: 88.51, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const setYt2 = [
      { value: 9.85, radius: 3 },
      { value: 17.05, radius: 3 },
      { value: 24.12, radius: 3 },
      { value: 31.31, radius: 3 },
      { value: 81.31, radius: 3 },
      { value: 88.51, radius: 3 },
    ];
    const yt2x = realWidth * Utils.sizeCoef * 18 / 100;

    Utils.createSetOfPortsCustomX(this, setYt2, yt2x, realWidth, realHeight, 0);

    const setYb2 = [
      { value: 9.85, radius: 3 },
      { value: 17.05, radius: 3 },
      { value: 24.12, radius: 3 },
      { value: 31.31, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 82 / 100;
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);

    const y = 0.60 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'ПЛК-150', fontSize: 10 })
  },
});
 const smartHome__satel__KNX_BSA12H = defaultShape.extend({

  NAME: "smartHome__satel__KNX_BSA12H",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 92;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/satel/KNX_BSA12H/icon.png?itok=_tCuPpOY",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.22, radius: 5 },
      { value: 23.63, radius: 5 },
      { value: 34.32, radius: 5 },
      { value: 44.87, radius: 5 },
      { value: 55.13, radius: 5 },
      { value: 65.82, radius: 5 },
      { value: 76.23, radius: 5 },
      { value: 86.78, radius: 5 },
    ];

    const setYb = [
      { value: 76.37, radius: 3 },
      { value: 84.11, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.1 * realWidth * Utils.sizeCoef;
    const y = 0.86 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'KNX-BSA12H' })
  },
});
 const smartHome__satel__KNX_BSA12L = defaultShape.extend({

  NAME: "smartHome__satel__KNX_BSA12L",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 92;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/satel/KNX_BSA12L/icon.png?itok=tpTalIql",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.22, radius: 5 },
      { value: 23.63, radius: 5 },
      { value: 34.32, radius: 5 },
      { value: 44.87, radius: 5 },
      { value: 55.13, radius: 5 },
      { value: 65.82, radius: 5 },
      { value: 76.23, radius: 5 },
      { value: 86.78, radius: 5 },
    ];

    const setYb = [
      { value: 76.37, radius: 3 },
      { value: 84.11, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.1 * realWidth * Utils.sizeCoef;
    const y = 0.86 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'KNX-BSA12L' })
  },
});
 const smartHome__satel__KNX_DIM21 = defaultShape.extend({

  NAME: "smartHome__satel__KNX_DIM21",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 92;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/satel/KNX_DIM21/icon.png?itok=3dTdu_G2",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.22, radius: 5 },
      { value: 23.63, radius: 5 },
      { value: 34.32, radius: 5 },
      { value: 44.87, radius: 5 },
      { value: 55.13, radius: 5 },
      { value: 65.82, radius: 5 },
      { value: 76.23, radius: 5 },
      { value: 86.78, radius: 5 },
    ];

    const setYb = [
      { value: 76.37, radius: 3 },
      { value: 84.11, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.1 * realWidth * Utils.sizeCoef;
    const y = 0.86 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'KNX-DIM21' })
  },
});
 const smartHome__satel__KNX_SA24 = defaultShape.extend({

  NAME: "smartHome__satel__KNX_SA24",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 92;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/satel/KNX_SA24/icon.png?itok=h6SQtYxr",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.22, radius: 3 },
      { value: 19.97, radius: 3 },
      { value: 26.58, radius: 3 },
      { value: 33.33, radius: 3 },
      { value: 40.08, radius: 3 },
      { value: 46.69, radius: 3 },
      { value: 53.45, radius: 3 },
      { value: 60.06, radius: 3 },
      { value: 66.67, radius: 3 },
      { value: 73.28, radius: 3 },
      { value: 80.17, radius: 3 },
      { value: 86.92, radius: 3 },
    ];

    const setYb = [
      { value: 16.46, radius: 3 },
      { value: 25.46, radius: 3 },
      { value: 75.81, radius: 3 },
      { value: 84.39, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.1 * realWidth * Utils.sizeCoef;
    const y = 0.86 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'KNX-SA24' })
  },
});
 const smartHome__satel__KNX_SA41 = defaultShape.extend({

  NAME: "smartHome__satel__KNX_SA41",

  init: function (attr) {
    const realWidth = 70;
    const realHeight = 92;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/satel/KNX_SA41/icon.png?itok=htGmnAZv",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.22, radius: 5 },
      { value: 23.63, radius: 5 },
      { value: 34.32, radius: 5 },
      { value: 44.87, radius: 5 },
      { value: 55.13, radius: 5 },
      { value: 65.82, radius: 5 },
      { value: 76.23, radius: 5 },
      { value: 86.78, radius: 5 },
    ];

    const setYb = [
      { value: 16.46, radius: 3 },
      { value: 25.46, radius: 3 },
      { value: 75.81, radius: 3 },
      { value: 84.39, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.1 * realWidth * Utils.sizeCoef;
    const y = 0.86 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: 'KNX-SA41' })
  },
});
 const smartHome__Siemens_LOGO__230RCE = defaultShape.extend({

  NAME: "smartHome__Siemens_LOGO__230RCE",

  init: function (attr) {
    const realWidth = 71.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Siemens_LOGO/230RCE/icon.png?itok=f7dHV1Bv",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 14.42, radius: 3 },
      { value: 21.63, radius: 3 },
      { value: 28.65, radius: 3 },
      { value: 35.49, radius: 3 },
    ];

    const setYb = [
      { value: 14.42, radius: 3 },
      { value: 21.63, radius: 3 },
      { value: 35.49, radius: 3 },
      { value: 42.88, radius: 3 },
      { value: 56.75, radius: 3 },
      { value: 64.14, radius: 3 },
      { value: 78.00, radius: 3 },
      { value: 85.21, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__Siemens_LOGO__DM16_230R = defaultShape.extend({

  NAME: "smartHome__Siemens_LOGO__DM16_230R",

  init: function (attr) {
    const realWidth = 71.5;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/Siemens_LOGO/DM16_230R/icon.png?itok=YjEkWSqo",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 21.81, radius: 3 },
      { value: 28.65, radius: 3 },
      { value: 35.86, radius: 3 },
      { value: 42.70, radius: 3 },
      { value: 49.91, radius: 3 },
      { value: 56.93, radius: 3 },
      { value: 63.96, radius: 3 },
      { value: 70.79, radius: 3 },
      { value: 78.00, radius: 3 },
      { value: 85.03, radius: 3 },
    ];

    const setYb = [
      { value: 7.58, radius: 3 },
      { value: 14.60, radius: 3 },
      { value: 28.65, radius: 3 },
      { value: 35.67, radius: 3 },
      { value: 56.75, radius: 3 },
      { value: 63.77, radius: 3 },
      { value: 78.00, radius: 3 },
      { value: 85.21, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const setYb2 = [
      { value: 14.79, radius: 3 },
      { value: 21.63, radius: 3 },
      { value: 35.86, radius: 3 },
      { value: 42.88, radius: 3 },
      { value: 56.93, radius: 3 },
      { value: 63.96, radius: 3 },
      { value: 77.82, radius: 3 },
      { value: 85.03, radius: 3 },
    ];
    const yb2x = realWidth * Utils.sizeCoef * 79 / 100;
    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);

    const y = 0.50 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'DM16 230R', fontSize: 10 })
  },
});
 const smartHome__smart_electronics__Alpha_SE_Hub2 = defaultShape.extend({

  NAME: "smartHome__smart_electronics__Alpha_SE_Hub2",

  init: function (attr) {
    const realWidth = 35;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/Alpha_SE_Hub2/icon.png?itok=NIdIKUI2",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 29.09, radius: 5 },
      { value: 43.21, radius: 5 },
      { value: 57.06, radius: 5 },
      { value: 70.91, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
  },
});
 const smartHome__smart_electronics__SE6i5o_Light = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SE6i5o_Light",

  init: function (attr) {
    const realWidth = 84;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SE6i5o%20Light/icon.png?itok=vxyLeR3g",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 6.93, radius: 5 },
      { value: 13.16, radius: 5 },
      { value: 19.27, radius: 5 },
      { value: 25.26, radius: 5 },
      { value: 31.61, radius: 5 },
      { value: 37.72, radius: 5 },
      { value: 43.83, radius: 5 },
      { value: 50.06, radius: 5 },
      { value: 56.29, radius: 5 },
      { value: 62.40, radius: 5 },
      { value: 74.74, radius: 5 },
      { value: 81.08, radius: 5 },
      { value: 87.19, radius: 5 },
      { value: 93.18, radius: 5 },
    ];

    const setYb = [
      { value: 6.82, radius: 5 },
      { value: 12.93, radius: 5 },
      { value: 19.27, radius: 5 },
      { value: 25.38, radius: 5 },
      { value: 31.49, radius: 5 },
      { value: 37.60, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 49.82, radius: 5 },
      { value: 56.29, radius: 5 },
      { value: 62.40, radius: 5 },
      { value: 68.63, radius: 5 },
      { value: 74.74, radius: 5 },
      { value: 80.85, radius: 5 },
      { value: 86.96, radius: 5 },
      { value: 93.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SE8i = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SE8i",

  init: function (attr) {
    const realWidth = 71;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SE8i/icon.png?itok=bwDIDaAN",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.68, radius: 5 },
      { value: 17.61, radius: 5 },
      { value: 24.69, radius: 5 },
      { value: 32.18, radius: 5 },
      { value: 39.11, radius: 5 },
      { value: 46.46, radius: 5 },
      { value: 60.61, radius: 5 },
      { value: 67.82, radius: 5 },
      { value: 74.90, radius: 5 },
      { value: 82.11, radius: 5 },
    ];

    const setYb = [
      { value: 10.68, radius: 5 },
      { value: 17.75, radius: 5 },
      { value: 24.97, radius: 5 },
      { value: 32.04, radius: 5 },
      { value: 39.25, radius: 5 },
      { value: 46.32, radius: 5 },
      { value: 75.03, radius: 5 },
      { value: 82.11, radius: 5 },
      { value: 89.04, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SEAquaControl = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SEAquaControl",

  init: function (attr) {
    const realWidth = 71;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SEAquaControl/icon.png?itok=sIt7n_xh",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 19.42, radius: 5 },
      { value: 26.49, radius: 5 },
      { value: 33.70, radius: 5 },
      { value: 41.19, radius: 5 },
      { value: 48.27, radius: 5 },
      { value: 55.48, radius: 5 },
      { value: 70.32, radius: 5 },
      { value: 77.25, radius: 5 },
      { value: 84.74, radius: 5 },
      { value: 92.09, radius: 5 },
    ];

    const setYb = [
      { value: 19.28, radius: 5 },
      { value: 26.49, radius: 5 },
      { value: 33.43, radius: 5 },
      { value: 40.78, radius: 5 },
      { value: 48.27, radius: 5 },
      { value: 55.76, radius: 5 },
      { value: 62.97, radius: 5 },
      { value: 70.04, radius: 5 },
      { value: 77.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SEBPWR = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SEBPWR",

  init: function (attr) {
    const realWidth = 71;
    const realHeight = 91;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SEBPWR/icon.png?itok=XXj7GJRs",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.54, radius: 5 },
      { value: 17.75, radius: 5 },
      { value: 24.83, radius: 5 },
      { value: 31.76, radius: 5 },
      { value: 38.97, radius: 5 },
      { value: 46.32, radius: 5 },
      { value: 53.40, radius: 5 },
      { value: 60.61, radius: 5 },
      { value: 67.68, radius: 5 },
      { value: 75.03, radius: 5 },
      { value: 81.97, radius: 5 },
      { value: 89.32, radius: 5 },
    ];

    const setYb = [
      { value: 10.54, radius: 5 },
      { value: 17.75, radius: 5 },
      { value: 24.83, radius: 5 },
      { value: 31.76, radius: 5 },
      { value: 38.97, radius: 5 },
      { value: 46.32, radius: 5 },
      { value: 53.40, radius: 5 },
      { value: 60.61, radius: 5 },
      { value: 67.68, radius: 5 },
      { value: 75.03, radius: 5 },
      { value: 81.97, radius: 5 },
      { value: 89.32, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SECurtain_DIN = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SECurtain_DIN",

  init: function (attr) {
    const realWidth = 84;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SECurtain_DIN/icon.png?itok=aAblk-S6",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 6.93, radius: 5 },
      { value: 13.16, radius: 5 },
      { value: 19.04, radius: 5 },
      { value: 25.62, radius: 5 },
      { value: 31.37, radius: 5 },
      { value: 37.96, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 49.82, radius: 5 },
      { value: 56.40, radius: 5 },
      { value: 62.51, radius: 5 },
      { value: 74.62, radius: 5 },
      { value: 80.85, radius: 5 },
      { value: 87.07, radius: 5 },
      { value: 93.30, radius: 5 },
    ];

    const setYb = [
      {value: 25.62, radius: 5},
      {value: 31.37, radius: 5},
      {value: 37.96, radius: 5},
      {value: 43.95, radius: 5},
      {value: 49.82, radius: 5},
      {value: 56.40, radius: 5},
      {value: 62.51, radius: 5},
      {value: 68.63, radius: 5},
      {value: 74.62, radius: 5},
      {value: 80.85, radius: 5},
      {value: 87.07, radius: 5},
      {value: 93.30, radius: 5},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SEDim2o = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SEDim2o",

  init: function (attr) {
    const realWidth = 84;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SEDim2o/icon.png?itok=Mlf0CKfx",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.28, radius: 5 },
      { value: 18.92, radius: 5 },
      { value: 25.15, radius: 5 },
      { value: 31.73, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 50.18, radius: 5 },
      { value: 56.29, radius: 5 },
      { value: 74.74, radius: 5 },
      { value: 81.08, radius: 5 },
      { value: 87.07, radius: 5 },
      { value: 93.07, radius: 5 },
    ];

    const setYb = [
      { value: 19.27, radius: 5 },
      { value: 25.26, radius: 5 },
      { value: 31.73, radius: 5 },
      { value: 37.60, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 50.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SERGBW = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SERGBW",

  init: function (attr) {
    const realWidth = 84;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SERGBW/icon.png?itok=9iqWfScO",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.04, radius: 5 },
      { value: 19.27, radius: 5 },
      { value: 25.26, radius: 5 },
      { value: 31.61, radius: 5 },
      { value: 37.84, radius: 5 },
      { value: 43.71, radius: 5 },
      { value: 50.18, radius: 5 },
      { value: 56.52, radius: 5 },
      { value: 74.62, radius: 5 },
      { value: 80.96, radius: 5 },
      { value: 87.07, radius: 5 },
      { value: 93.18, radius: 5 },
    ];

    const setYb = [
      { value: 50.06, radius: 5 },
      { value: 56.29, radius: 5 },
      { value: 62.51, radius: 5 },
      { value: 68.63, radius: 5 },
      { value: 74.74, radius: 5 },
      { value: 80.96, radius: 5 },
      { value: 87.19, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SESecu = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SESecu",

  init: function (attr) {
    const realWidth = 84;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SESecu/icon.png?itok=jACmBGm3",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 6.70, radius: 5 },
      { value: 12.93, radius: 5 },
      { value: 19.15, radius: 5 },
      { value: 25.38, radius: 5 },
      { value: 37.60, radius: 5 },
      { value: 49.82, radius: 5 },
      { value: 56.17, radius: 5 },
      { value: 62.40, radius: 5 },
      { value: 74.62, radius: 5 },
      { value: 80.85, radius: 5 },
      { value: 86.96, radius: 5 },
      { value: 93.18, radius: 5 },
    ];

    const setYb = [
      { value: 25.38, radius: 5 },
      { value: 31.49, radius: 5 },
      { value: 37.49, radius: 5 },
      { value: 43.95, radius: 5 },
      { value: 50.06, radius: 5 },
      { value: 56.17, radius: 5 },
      { value: 62.28, radius: 5 },
      { value: 68.63, radius: 5 },
      { value: 74.50, radius: 5 },
      { value: 80.85, radius: 5 },
      { value: 86.96, radius: 5 },
      { value: 93.07, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__smart_electronics__SE_Thermo2o = defaultShape.extend({

  NAME: "smartHome__smart_electronics__SE_Thermo2o",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;
    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/smart_electronics/SE%20Thermo2o/icon.png?itok=lOPOx_AB",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 13.31, radius: 3 },
      { value: 17.93, radius: 3 },
      { value: 22.55, radius: 3 },
      { value: 27.54, radius: 3 },
      { value: 32.35, radius: 3 },
      { value: 36.78, radius: 3 },
      { value: 41.59, radius: 3 },
      { value: 46.40, radius: 3 },
      { value: 59.70, radius: 3 },
      { value: 69.13, radius: 3 },
      { value: 78.37, radius: 3 },
      { value: 87.80, radius: 3 },
    ];

    const setYb = [
      { value: 11.46, radius: 5 },
      { value: 20.89, radius: 5 },
      { value: 30.31, radius: 5 },
      { value: 39.93, radius: 5 },
      { value: 49.35, radius: 5 },
      { value: 59.15, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__zont__E_BUS_Ariston_DIN_725 = defaultShape.extend({

  NAME: "smartHome__zont__E_BUS_Ariston_DIN_725",

  init: function (attr) {
    const realWidth = 50;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/zont/E_BUS_Ariston_DIN_725/icon.png?itok=lRLLTimS",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 69.54, radius: 3 },
      { value: 78.90, radius: 3 },
      { value: 88.25, radius: 3 },
    ];

    const setYb = [
      { value: 33.81, radius: 3 },
      { value: 42.45, radius: 3 },
      { value: 62.35, radius: 3 },
      { value: 71.22, radius: 3 },
      { value: 79.62, radius: 3 },
      { value: 88.49, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const smartHome__zont__H1000_plus = defaultShape.extend({

  NAME: "smartHome__zont__H1000_plus",

  init: function (attr) {
    const realWidth = 105;
    const realHeight = 91;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/smartHome/zont/H1000_plus/icon.png?itok=w9HxjF0e",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 21.21, radius: 5 },
      { value: 30.93, radius: 5 },
      { value: 40.66, radius: 5 },
      { value: 55.68, radius: 3 },
      { value: 58.33, radius: 3 },
      { value: 61.36, radius: 3 },
      { value: 64.27, radius: 3 },
      { value: 67.30, radius: 3 },
      { value: 70.20, radius: 3 },
      { value: 74.49, radius: 3 },
      { value: 78.54, radius: 3 },
      { value: 82.83, radius: 3 },
    ];

    const setYb = [
      { value: 10.35, radius: 3 },
      { value: 14.65, radius: 3 },
      { value: 18.81, radius: 3 },
      { value: 23.23, radius: 3 },
      { value: 27.53, radius: 3 },
      { value: 31.94, radius: 3 },
      { value: 36.24, radius: 3 },
      { value: 40.53, radius: 3 },
      { value: 44.82, radius: 3 },
      { value: 54.67, radius: 3 },
      { value: 58.96, radius: 3 },
      { value: 63.26, radius: 3 },
      { value: 67.55, radius: 3 },
      { value: 71.97, radius: 3 },
      { value: 76.14, radius: 3 },
      { value: 80.30, radius: 3 },
      { value: 84.72, radius: 3 },
      { value: 89.14, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const switch__EKF__proxima__tpc63__pole_4__16a = defaultShape.extend({

  NAME: "switch__EKF__proxima__tpc63__pole_4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 72;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/TPC63/pole_4/icon.png?itok=unLYEQal",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.34, radius: 5 },
      { value: 43.07, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    const setYb = [
      { value: 8.29, radius: 5 },
      { value: 18.21, radius: 5 },
      { value: 32.74, radius: 5 },
      { value: 42.80, radius: 5 },
      { value: 57.47, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 81.79, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const switch__EKF__proxima__tpc63__pole_4__25a = defaultShape.extend({

  NAME: "switch__EKF__proxima__tpc63__pole_4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 72;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/TPC63/pole_4/icon.png?itok=unLYEQal",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.34, radius: 5 },
      { value: 43.07, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    const setYb = [
      { value: 8.29, radius: 5 },
      { value: 18.21, radius: 5 },
      { value: 32.74, radius: 5 },
      { value: 42.80, radius: 5 },
      { value: 57.47, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 81.79, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const switch__EKF__proxima__tpc63__pole_4__63a = defaultShape.extend({

  NAME: "switch__EKF__proxima__tpc63__pole_4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 72;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/TPC63/pole_4/icon.png?itok=unLYEQal",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.34, radius: 5 },
      { value: 43.07, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    const setYb = [
      { value: 8.29, radius: 5 },
      { value: 18.21, radius: 5 },
      { value: 32.74, radius: 5 },
      { value: 42.80, radius: 5 },
      { value: 57.47, radius: 5 },
      { value: 67.80, radius: 5 },
      { value: 81.79, radius: 5 },
      { value: 92.39, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.75 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__16a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__16a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__20a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__20a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '20A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__32a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__32a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__40a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__40a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__63a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__63a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__100a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__100a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '100A' })
  },
});
 const switch__EKF__proxima__BH_63__p2__125a = defaultShape.extend({

  NAME: "switch__EKF__proxima__BH_63__p2__125a",

  init: function (attr) {
    const realWidth = 35.6;
    const realHeight = 87.5;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/EKF/PROxima/%D0%92%D0%9D63/pole_2/icon.png?itok=tXJJlaO1",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    const setYb = [
      { value: 27.13, radius: 5 },
      { value: 74.68, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = -0.25 * realWidth * Utils.sizeCoef;
    const y = 0.45 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '125A' })
  },
});
 const switch__ekm__rp__rp_1__40 = defaultShape.extend({

  NAME: "switch__ekm__rp__rp_1__40",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/ekm/RP/rp_1/40/icon.png?itok=HPpsBeNA",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 70, radius: 5},
    ];

    const setYb = [
      {value: 30, radius: 5},
      {value: 70, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.72 * realHeight * Utils.sizeCoef;
    const y2 = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, {x: 0, y, text: 'РП-1'})
    Utils.createLabel(this, {x: 0, y: y2, text: '40'})
  },
});
 const switch__ekm__rp__rp_2__40 = defaultShape.extend({

  NAME: "switch__ekm__rp__rp_2__40",

  init: function (attr) {
    const realWidth = 18 * 2;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/ekm/RP/rp_2/40/icon.png?itok=0tfmiNWy",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 36.12, radius: 5 },
      { value: 84.64, radius: 5 },
    ];

    const setYb = [
      { value: 15.09, radius: 5 },
      { value: 36.12, radius: 5 },
      { value: 64.15, radius: 5 },
      { value: 84.64, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'РП-2-40' })
  },
});
 const switch__ekm__rp__rp_3__40 = defaultShape.extend({

  NAME: "switch__ekm__rp__rp_3__40",

  init: function (attr) {
    const realWidth = 18 * 3;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/ekm/RP/rp_3/40/icon.png?itok=lZE-0x28",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 24.32, radius: 5 },
      { value: 57.17, radius: 5 },
      { value: 89.84, radius: 5 },
    ];

    const setYb = [
      { value: 10.34, radius: 5 },
      { value: 24.32, radius: 5 },
      { value: 43.19, radius: 5 },
      { value: 57.17, radius: 5 },
      { value: 75.86, radius: 5 },
      { value: 89.84, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'РП-3-40' })
  },
});
 const switch__ekm__rp__rp_4__40 = defaultShape.extend({

  NAME: "switch__ekm__rp__rp_4__40",

  init: function (attr) {
    const realWidth = 18 * 4;
    const realHeight = 83;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/ekm/RP/rp_4/40/icon.png?itok=9yBaDEUt",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 18.47, radius: 5 },
      { value: 42.95, radius: 5 },
      { value: 67.58, radius: 5 },
      { value: 92.20, radius: 5 },
    ];

    const setYb = [
      { value: 7.93, radius: 5 },
      { value: 18.47, radius: 5 },
      { value: 32.42, radius: 5 },
      { value: 42.95, radius: 5 },
      { value: 57.05, radius: 5 },
      { value: 67.58, radius: 5 },
      { value: 81.67, radius: 5 },
      { value: 92.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: 'РП-4-40' })
  },
});
 const switch__hager__SBN263 = defaultShape.extend({

  NAME: "switch__hager__SBN263",

  init: function (attr) {
    const din = 2;
    const realWidth = 17.5 * din;
    const realHeight = 85;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/Hager/SBN263/icon.png?itok=pEEIWN47",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    const setYb = [
      { value: 25, radius: 5 },
      { value: 75, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const switch__iek__BPT__p1__16a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__16a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
  },
});
 const switch__iek__BPT__p1__25a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__25a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__iek__BPT__p1__32a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__32a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__iek__BPT__p1__40a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__40a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__iek__BPT__p1__50a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__50a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '50A' })
  },
});
 const switch__iek__BPT__p1__63a = defaultShape.extend({

  NAME: "switch__iek__BPT__p1__63a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 88

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_1/icon.png?itok=XosEHoLX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 72.25, radius: 5 },
    ];

    const setYb = [
      { value: 27.75, radius: 5 },
      { value: 72.25, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__iek__BPT__p2__16a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__16a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const switch__iek__BPT__p2__25a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__25a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const switch__iek__BPT__p2__32a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__32a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const switch__iek__BPT__p2__40a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__40a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const switch__iek__BPT__p2__50a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__50a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const switch__iek__BPT__p2__63a = defaultShape.extend({

  NAME: "switch__iek__BPT__p2__63a",

  init: function (attr) {
    const din = 2;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_2/icon.png?itok=HuuW2jdT",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 37.20, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    const setYb = [
      { value: 14.29, radius: 5 },
      { value: 37.20, radius: 5 },
      { value: 62.80, radius: 5 },
      { value: 85.98, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const switch__iek__BPT__p3__16a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__16a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })
  },
});
 const switch__iek__BPT__p3__25a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__25a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })
  },
});
 const switch__iek__BPT__p3__32a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__32a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })
  },
});
 const switch__iek__BPT__p3__40a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__40a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })
  },
});
 const switch__iek__BPT__p3__50a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__50a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })
  },
});
 const switch__iek__BPT__p3__63a = defaultShape.extend({

  NAME: "switch__iek__BPT__p3__63a",

  init: function (attr) {
    const din = 3;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_3/icon.png?itok=CPK0ufS1",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 25.05, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    const setYb = [
      { value: 9.62, radius: 5 },
      { value: 25.05, radius: 5 },
      { value: 42.11, radius: 5 },
      { value: 57.71, radius: 5 },
      { value: 74.77, radius: 5 },
      { value: 90.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })
  },
});
 const switch__iek__BPT__p4__16a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__16a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '16A' })

  },
});
 const switch__iek__BPT__p4__25a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__25a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '25A' })

  },
});
 const switch__iek__BPT__p4__32a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__32a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '32A' })

  },
});
 const switch__iek__BPT__p4__40a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__40a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '40A' })

  },
});
 const switch__iek__BPT__p4__50a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__50a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '50A' })

  },
});
 const switch__iek__BPT__p4__63a = defaultShape.extend({

  NAME: "switch__iek__BPT__p4__63a",

  init: function (attr) {
    const din = 4;
    const realWidth = 18 * din;
    const realHeight = 88;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/BPT/pole_4/icon.png?itok=RotrDcak",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 18.74, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    const setYb = [
      { value: 6.98, radius: 5 },
      { value: 18.74, radius: 5 },
      { value: 32.01, radius: 5 },
      { value: 43.37, radius: 5 },
      { value: 56.50, radius: 5 },
      { value: 67.99, radius: 5 },
      { value: 80.98, radius: 5 },
      { value: 92.61, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const x = (realWidth * Utils.sizeCoef / din) * -(din - 1) / 2;
    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x, y, text: '63A' })

  },
});
 const switch__iek__karat__MAT10_063 = defaultShape.extend({

  NAME: "switch__iek__karat__MAT10_063",

  init: function (attr) {
    const realWidth = 248;
    const realHeight = 125;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/karat/MAT10_063/icon.png?itok=SPOfKxpi",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 10.65, radius: 5},
      {value: 19.54, radius: 5},
      {value: 28.34, radius: 5},
      {value: 35.46, radius: 3}, // 29,04 15 1,815
      // ...
      {value: 64.50, radius: 3},
      {value: 71.66, radius: 5},
      {value: 80.58, radius: 5},
      {value: 89.43, radius: 5},
    ];

    let startPoint = 35.46;
    for (let i=1;i<16;i++) {
      startPoint+=1.815;
      setYt.push({value: startPoint, radius: 3});
    }

    const setYb = [
      {value: 10.65, radius: 5},
      {value: 19.54, radius: 5},
      {value: 28.34, radius: 5},
      {value: 71.66, radius: 5},
      {value: 80.58, radius: 5},
      {value: 89.43, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

  },
});
 const switch__iek__ВH_32__p1__20a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__20a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
  },
});
 const switch__iek__ВH_32__p1__25a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__25a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__iek__ВH_32__p1__32a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__32a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__iek__ВH_32__p1__40a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__40a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__iek__ВH_32__p1__63a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__63a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__iek__ВH_32__p1__100a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p1__100a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_1/icon.png?itok=m0akjkRf",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 50.80, radius: 5 },
    ];

    const setYb = [
      { value: 50.80, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '100A' })
  },
});
 const switch__iek__ВH_32__p2__20a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__20a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
  },
});
 const switch__iek__ВH_32__p2__25a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__25a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__iek__ВH_32__p2__32a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__32a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__iek__ВH_32__p2__40a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__40a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__iek__ВH_32__p2__63a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__63a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__iek__ВH_32__p2__100a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p2__100a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_2/icon.png?itok=6WCbBW90",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    const setYb = [
      { value: 25.89, radius: 5 },
      { value: 75.20, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '100A' })
  },
});
 const switch__iek__ВH_32__p3__20a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__20a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
  },
});
 const switch__iek__ВH_32__p3__25a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__25a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__iek__ВH_32__p3__32a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__32a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__iek__ВH_32__p3__40a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__40a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__iek__ВH_32__p3__63a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__63a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__iek__ВH_32__p3__100a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p3__100a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_3/icon.png?itok=iXUetXAe",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    const setYb = [
      { value: 17.55, radius: 5 },
      { value: 50.64, radius: 5 },
      { value: 83.18, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '100A' })
  },
});
 const switch__iek__ВH_32__p4__20a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__20a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '20A' })
  },
});
 const switch__iek__ВH_32__p4__25a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__25a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__iek__ВH_32__p4__32a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__32a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__iek__ВH_32__p4__40a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__40a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__iek__ВH_32__p4__63a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__63a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__iek__ВH_32__p4__100a = defaultShape.extend({

  NAME: "switch__iek__ВH_32__p4__100a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 74;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/iek/%D0%92%D0%9D_32/pole_4/icon.png?itok=Qx2cWvhN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    const setYb = [
      { value: 12.93, radius: 5 },
      { value: 37.55, radius: 5 },
      { value: 62.45, radius: 5 },
      { value: 87.48, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.79 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '100A' })
  },
});
 const switch__omix__ats3_3 = defaultShape.extend({

  NAME: "switch__omix__ats3_3",

  init: function (attr) {
    const realWidth = 222;
    const realHeight = 125;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/OMIX/ATS3_3/icon.png?itok=FHz905O9",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 74.21, radius: 5 },
      { value: 81.87, radius: 5 },
      { value: 89.60, radius: 5 },
      { value: 10.47, radius: 5 },
      { value: 18.13, radius: 5 },
      { value: 25.92, radius: 5 },
    ];

    const setYb = [
      { value: 74.21, radius: 5 },
      { value: 81.87, radius: 5 },
      { value: 89.60, radius: 5 },
      { value: 10.47, radius: 5 },
      { value: 18.13, radius: 5 },
      { value: 25.92, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const switch__TDM__MP_63__p1__16a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p1__16a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_1/icon.png?itok=6SOUgwEG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 71.12, radius: 5 },
    ];

    const setYb = [
      { value: 29.95, radius: 5 },
      { value: 71.12, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
  },
});
 const switch__TDM__MP_63__p1__25a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p1__25a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_1/icon.png?itok=6SOUgwEG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 71.12, radius: 5 },
    ];

    const setYb = [
      { value: 29.95, radius: 5 },
      { value: 71.12, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__TDM__MP_63__p1__32a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p1__32a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_1/icon.png?itok=6SOUgwEG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 71.12, radius: 5 },
    ];

    const setYb = [
      { value: 29.95, radius: 5 },
      { value: 71.12, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__TDM__MP_63__p1__40a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p1__40a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_1/icon.png?itok=6SOUgwEG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 71.12, radius: 5 },
    ];

    const setYb = [
      { value: 29.95, radius: 5 },
      { value: 71.12, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__TDM__MP_63__p1__63a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p1__63a",

  init: function (attr) {
    const realWidth = 18;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_1/icon.png?itok=6SOUgwEG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 71.12, radius: 5 },
    ];

    const setYb = [
      { value: 29.95, radius: 5 },
      { value: 71.12, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__TDM__MP_63__p2__16a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p2__16a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_2/icon.png?itok=SEo41R0v",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 35.97, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    const setYb = [
      { value: 14.44, radius: 5 },
      { value: 35.97, radius: 5 },
      { value: 64.58, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '16A' })
  },
});
 const switch__TDM__MP_63__p2__25a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p2__25a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_2/icon.png?itok=SEo41R0v",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 35.97, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    const setYb = [
      { value: 14.44, radius: 5 },
      { value: 35.97, radius: 5 },
      { value: 64.58, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '25A' })
  },
});
 const switch__TDM__MP_63__p2__32a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p2__32a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_2/icon.png?itok=SEo41R0v",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 35.97, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    const setYb = [
      { value: 14.44, radius: 5 },
      { value: 35.97, radius: 5 },
      { value: 64.58, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '32A' })
  },
});
 const switch__TDM__MP_63__p2__40a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p2__40a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_2/icon.png?itok=SEo41R0v",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 35.97, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    const setYb = [
      { value: 14.44, radius: 5 },
      { value: 35.97, radius: 5 },
      { value: 64.58, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__TDM__MP_63__p2__63a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p2__63a",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_2/icon.png?itok=SEo41R0v",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 35.97, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    const setYb = [
      { value: 14.44, radius: 5 },
      { value: 35.97, radius: 5 },
      { value: 64.58, radius: 5 },
      { value: 85.29, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__TDM__MP_63__p3__40a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p3__40a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_3/icon.png?itok=YuPSKSeD",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 24.50, radius: 5 },
      { value: 57.22, radius: 5 },
      { value: 90.31, radius: 5 },
    ];

    const setYb = [
      { value: 10.05, radius: 5 },
      { value: 24.13, radius: 5 },
      { value: 43.14, radius: 5 },
      { value: 57.04, radius: 5 },
      { value: 76.05, radius: 5 },
      { value: 90.13, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '40A' })
  },
});
 const switch__TDM__MP_63__p3__63a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p3__63a",

  init: function (attr) {
    const realWidth = 54;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_3/icon.png?itok=YuPSKSeD",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 24.50, radius: 5 },
      { value: 57.22, radius: 5 },
      { value: 90.31, radius: 5 },
    ];

    const setYb = [
      { value: 10.05, radius: 5 },
      { value: 24.13, radius: 5 },
      { value: 43.14, radius: 5 },
      { value: 57.04, radius: 5 },
      { value: 76.05, radius: 5 },
      { value: 90.13, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__TDM__MP_63__p4__63a = defaultShape.extend({

  NAME: "switch__TDM__MP_63__p4__63a",

  init: function (attr) {
    const realWidth = 72;
    const realHeight = 75;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TDM/MP_63/pole_4/icon.png?itok=5m8OTxkr",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 18.16, radius: 5 },
      { value: 43.19, radius: 5 },
      { value: 67.95, radius: 5 },
      { value: 92.57, radius: 5 },
    ];

    const setYb = [
      { value: 7.70, radius: 5 },
      { value: 18.16, radius: 5 },
      { value: 32.74, radius: 5 },
      { value: 43.19, radius: 5 },
      { value: 57.22, radius: 5 },
      { value: 67.95, radius: 5 },
      { value: 81.98, radius: 5 },
      { value: 92.57, radius: 5 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    const y = 0.76 * realHeight * Utils.sizeCoef;
    Utils.createLabel(this, { x: 0, y, text: '63A' })
  },
});
 const switch__TOMZN__TOQ5_100PV_4 = defaultShape.extend({

  NAME: "switch__TOMZN__TOQ5_100PV_4",

  init: function (attr) {
    const realWidth = 141;
    const realHeight = 104;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/switch/TOMZN/TOQ5_100PV_4/icon.png?itok=ERYPWKwO",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 5.24, radius: 3},
      {value: 8.22, radius: 3},
      {value: 11.13, radius: 3},
      {value: 14.10, radius: 3},
      {value: 17.15, radius: 3},
      {value: 53.51, radius: 5},
      {value: 66.48, radius: 5},
      {value: 79.38, radius: 5},
      {value: 92.35, radius: 5},
    ];

    const setYb = [
      {value: 5.24, radius: 3},
      {value: 8.22, radius: 3},
      {value: 11.13, radius: 3},
      {value: 14.10, radius: 3},
      {value: 17.15, radius: 3},
      {value: 53.51, radius: 5},
      {value: 66.48, radius: 5},
      {value: 79.38, radius: 5},
      {value: 92.35, radius: 5},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);

    // Second
    const setYb2 = [
      {value: 53.51, radius: 5},
      {value: 66.48, radius: 5},
      {value: 79.38, radius: 5},
      {value: 92.35, radius: 5},
    ];
    const yb2x = realWidth * Utils.sizeCoef * 79 / 100;

    Utils.createSetOfPortsCustomX(this, setYb2, yb2x, realWidth, realHeight, 2);


  },
});
 const terminals__DART__p2 = defaultShape.extend({

  NAME: "terminals__DART__p2",

  init: function (attr) {
    const realWidth = 11.96;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p2/icon.png?itok=agWGE86U",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 29.92, radius: 3 },
      { value: 70.08, radius: 3 },
    ];

    const setYb = [
      { value: 29.92, radius: 3 },
      { value: 70.08, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p3 = defaultShape.extend({

  NAME: "terminals__DART__p3",

  init: function (attr) {
    const realWidth = 17.04;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p3/icon.png?itok=X-NQF2UG",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 22.60, radius: 3 },
      { value: 50.85, radius: 3 },
      { value: 78.53, radius: 3 },
    ];

    const setYb = [
      { value: 22.60, radius: 3 },
      { value: 50.85, radius: 3 },
      { value: 78.53, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p4 = defaultShape.extend({

  NAME: "terminals__DART__p4",

  init: function (attr) {
    const realWidth = 22.12;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p4/icon.png?itok=VxK1u_H_",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 18.06, radius: 3},
      {value: 39.65, radius: 3},
      {value: 61.23, radius: 3},
      {value: 83.70, radius: 3},      
    ];

    const setYb = [
      {value: 18.06, radius: 3},
      {value: 39.65, radius: 3},
      {value: 61.23, radius: 3},
      {value: 83.70, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p5 = defaultShape.extend({

  NAME: "terminals__DART__p5",

  init: function (attr) {
    const realWidth = 27.2;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p5/icon.png?itok=K1YN_Nbp",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 14.80, radius: 3},
      {value: 32.49, radius: 3},
      {value: 50.54, radius: 3},
      {value: 68.23, radius: 3},
      {value: 86.64, radius: 3},
    ];

    const setYb = [
      {value: 14.80, radius: 3},
      {value: 32.49, radius: 3},
      {value: 50.54, radius: 3},
      {value: 68.23, radius: 3},
      {value: 86.64, radius: 3},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p6 = defaultShape.extend({

  NAME: "terminals__DART__p6",

  init: function (attr) {
    const realWidth = 32.28;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p6/icon.png?itok=Oo3es1VN",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 12.23, radius: 3 },
      { value: 27.52, radius: 3 },
      { value: 42.81, radius: 3 },
      { value: 58.10, radius: 3 },
      { value: 73.09, radius: 3 },
      { value: 88.38, radius: 3 },
    ];

    const setYb = [
      { value: 12.23, radius: 3 },
      { value: 27.52, radius: 3 },
      { value: 42.81, radius: 3 },
      { value: 58.10, radius: 3 },
      { value: 73.09, radius: 3 },
      { value: 88.38, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p7 = defaultShape.extend({

  NAME: "terminals__DART__p7",

  init: function (attr) {
    const realWidth = 37.36;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p7/icon.png?itok=SK0wNodm",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 10.61, radius: 3 },
      { value: 24.14, radius: 3 },
      { value: 36.87, radius: 3 },
      { value: 50.40, radius: 3 },
      { value: 63.40, radius: 3 },
      { value: 76.66, radius: 3 },
      { value: 90.45, radius: 3 },
    ];

    const setYb = [
      { value: 10.61, radius: 3 },
      { value: 24.14, radius: 3 },
      { value: 36.87, radius: 3 },
      { value: 50.40, radius: 3 },
      { value: 63.40, radius: 3 },
      { value: 76.66, radius: 3 },
      { value: 90.45, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p8 = defaultShape.extend({

  NAME: "terminals__DART__p8",

  init: function (attr) {
    const realWidth = 42.44;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p8/icon.png?itok=11XybKSd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 9.37, radius: 3},
      {value: 21.08, radius: 3},
      {value: 32.79, radius: 3},
      {value: 44.50, radius: 3},
      {value: 55.97, radius: 3},
      {value: 67.68, radius: 3},
      {value: 79.63, radius: 3},
      {value: 91.33, radius: 3},  
    ];

    const setYb = [
      {value: 9.37, radius: 3},
      {value: 21.08, radius: 3},
      {value: 32.79, radius: 3},
      {value: 44.50, radius: 3},
      {value: 55.97, radius: 3},
      {value: 67.68, radius: 3},
      {value: 79.63, radius: 3},
      {value: 91.33, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p9 = defaultShape.extend({

  NAME: "terminals__DART__p9",

  init: function (attr) {
    const realWidth = 47.52;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p9/icon.png?itok=XknMB9s3",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 8.39, radius: 3 },
      { value: 19.08, radius: 3 },
      { value: 29.56, radius: 3 },
      { value: 39.62, radius: 3 },
      { value: 50.10, radius: 3 },
      { value: 60.80, radius: 3 },
      { value: 71.28, radius: 3 },
      { value: 81.55, radius: 3 },
      { value: 92.24, radius: 3 },
    ];

    const setYb = [
      { value: 8.39, radius: 3 },
      { value: 19.08, radius: 3 },
      { value: 29.56, radius: 3 },
      { value: 39.62, radius: 3 },
      { value: 50.10, radius: 3 },
      { value: 60.80, radius: 3 },
      { value: 71.28, radius: 3 },
      { value: 81.55, radius: 3 },
      { value: 92.24, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p10 = defaultShape.extend({

  NAME: "terminals__DART__p10",

  init: function (attr) {
    const realWidth = 52.6;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p10/icon.png?itok=oNVAVQXX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 7.59, radius: 3 },
      { value: 16.89, radius: 3 },
      { value: 26.57, radius: 3 },
      { value: 35.86, radius: 3 },
      { value: 45.54, radius: 3 },
      { value: 55.22, radius: 3 },
      { value: 64.52, radius: 3 },
      { value: 74.00, radius: 3 },
      { value: 83.30, radius: 3 },
      { value: 92.79, radius: 3 },
    ];

    const setYb = [
      { value: 7.59, radius: 3 },
      { value: 16.89, radius: 3 },
      { value: 26.57, radius: 3 },
      { value: 35.86, radius: 3 },
      { value: 45.54, radius: 3 },
      { value: 55.22, radius: 3 },
      { value: 64.52, radius: 3 },
      { value: 74.00, radius: 3 },
      { value: 83.30, radius: 3 },
      { value: 92.79, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p11 = defaultShape.extend({

  NAME: "terminals__DART__p11",

  init: function (attr) {
    const realWidth = 57.68;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p11/icon.png?itok=3i3NRw8w",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 6.93, radius: 3 },
      { value: 15.77, radius: 3 },
      { value: 24.26, radius: 3 },
      { value: 32.93, radius: 3 },
      { value: 41.42, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 58.75, radius: 3 },
      { value: 67.42, radius: 3 },
      { value: 76.26, radius: 3 },
      { value: 84.92, radius: 3 },
      { value: 93.41, radius: 3 },
    ];

    const setYb = [
      { value: 6.93, radius: 3 },
      { value: 15.77, radius: 3 },
      { value: 24.26, radius: 3 },
      { value: 32.93, radius: 3 },
      { value: 41.42, radius: 3 },
      { value: 50.26, radius: 3 },
      { value: 58.75, radius: 3 },
      { value: 67.42, radius: 3 },
      { value: 76.26, radius: 3 },
      { value: 84.92, radius: 3 },
      { value: 93.41, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p12 = defaultShape.extend({

  NAME: "terminals__DART__p12",

  init: function (attr) {
    const realWidth = 62.76;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p12/icon.png?itok=pS31QIJt",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 6.06, radius: 3 },
      { value: 14.35, radius: 3 },
      { value: 22.33, radius: 3 },
      { value: 30.30, radius: 3 },
      { value: 38.12, radius: 3 },
      { value: 46.09, radius: 3 },
      { value: 54.23, radius: 3 },
      { value: 62.04, radius: 3 },
      { value: 70.02, radius: 3 },
      { value: 78.15, radius: 3 },
      { value: 86.12, radius: 3 },
      { value: 94.10, radius: 3 },
    ];

    const setYb = [
      { value: 6.06, radius: 3 },
      { value: 14.35, radius: 3 },
      { value: 22.33, radius: 3 },
      { value: 30.30, radius: 3 },
      { value: 38.12, radius: 3 },
      { value: 46.09, radius: 3 },
      { value: 54.23, radius: 3 },
      { value: 62.04, radius: 3 },
      { value: 70.02, radius: 3 },
      { value: 78.15, radius: 3 },
      { value: 86.12, radius: 3 },
      { value: 94.10, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p13 = defaultShape.extend({

  NAME: "terminals__DART__p13",

  init: function (attr) {
    const realWidth = 67.84;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p13/icon.png?itok=6fDPEJCa",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 5.61, radius: 3 },
      { value: 13.00, radius: 3 },
      { value: 20.38, radius: 3 },
      { value: 28.06, radius: 3 },
      { value: 35.45, radius: 3 },
      { value: 42.54, radius: 3 },
      { value: 50.22, radius: 3 },
      { value: 57.61, radius: 3 },
      { value: 64.70, radius: 3 },
      { value: 72.38, radius: 3 },
      { value: 79.76, radius: 3 },
      { value: 87.30, radius: 3 },
      { value: 94.53, radius: 3 },
    ];

    const setYb = [
      { value: 5.61, radius: 3 },
      { value: 13.00, radius: 3 },
      { value: 20.38, radius: 3 },
      { value: 28.06, radius: 3 },
      { value: 35.45, radius: 3 },
      { value: 42.54, radius: 3 },
      { value: 50.22, radius: 3 },
      { value: 57.61, radius: 3 },
      { value: 64.70, radius: 3 },
      { value: 72.38, radius: 3 },
      { value: 79.76, radius: 3 },
      { value: 87.30, radius: 3 },
      { value: 94.53, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p14 = defaultShape.extend({

  NAME: "terminals__DART__p14",

  init: function (attr) {
    const realWidth = 72.92;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p14/icon.png?itok=iOIyMp2l",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 5.36, radius: 3},
      {value: 12.24, radius: 3},
      {value: 19.26, radius: 3},
      {value: 26.13, radius: 3},
      {value: 32.87, radius: 3},
      {value: 39.61, radius: 3},
      {value: 46.49, radius: 3},
      {value: 53.51, radius: 3},
      {value: 60.39, radius: 3},
      {value: 67.40, radius: 3},
      {value: 74.28, radius: 3},
      {value: 81.02, radius: 3},
      {value: 88.17, radius: 3},
      {value: 94.91, radius: 3},      
    ];

    const setYb = [
      {value: 5.36, radius: 3},
      {value: 12.24, radius: 3},
      {value: 19.26, radius: 3},
      {value: 26.13, radius: 3},
      {value: 32.87, radius: 3},
      {value: 39.61, radius: 3},
      {value: 46.49, radius: 3},
      {value: 53.51, radius: 3},
      {value: 60.39, radius: 3},
      {value: 67.40, radius: 3},
      {value: 74.28, radius: 3},
      {value: 81.02, radius: 3},
      {value: 88.17, radius: 3},
      {value: 94.91, radius: 3},           
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p15 = defaultShape.extend({

  NAME: "terminals__DART__p15",

  init: function (attr) {
    const realWidth = 78;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p15/icon.png?itok=CmOXlgEx",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 5.02, radius: 3},
      {value: 11.33, radius: 3},
      {value: 17.89, radius: 3},
      {value: 24.45, radius: 3},
      {value: 30.63, radius: 3},
      {value: 36.94, radius: 3},
      {value: 43.89, radius: 3},
      {value: 49.81, radius: 3},
      {value: 56.24, radius: 3},
      {value: 62.81, radius: 3},
      {value: 69.24, radius: 3},
      {value: 75.68, radius: 3},
      {value: 82.24, radius: 3},
      {value: 88.67, radius: 3},
      {value: 95.11, radius: 3},      
    ];

    const setYb = [
      {value: 5.02, radius: 3},
      {value: 11.33, radius: 3},
      {value: 17.89, radius: 3},
      {value: 24.45, radius: 3},
      {value: 30.63, radius: 3},
      {value: 36.94, radius: 3},
      {value: 43.89, radius: 3},
      {value: 49.81, radius: 3},
      {value: 56.24, radius: 3},
      {value: 62.81, radius: 3},
      {value: 69.24, radius: 3},
      {value: 75.68, radius: 3},
      {value: 82.24, radius: 3},
      {value: 88.67, radius: 3},
      {value: 95.11, radius: 3},      
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p16 = defaultShape.extend({

  NAME: "terminals__DART__p16",

  init: function (attr) {
    const realWidth = 82.28;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p16/icon.png?itok=JQgI1-QM",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 4.84, radius: 3 },
      { value: 10.88, radius: 3 },
      { value: 16.81, radius: 3 },
      { value: 22.85, radius: 3 },
      { value: 28.78, radius: 3 },
      { value: 35.19, radius: 3 },
      { value: 41.11, radius: 3 },
      { value: 47.04, radius: 3 },
      { value: 52.84, radius: 3 },
      { value: 59.13, radius: 3 },
      { value: 65.42, radius: 3 },
      { value: 71.22, radius: 3 },
      { value: 77.39, radius: 3 },
      { value: 83.19, radius: 3 },
      { value: 89.48, radius: 3 },
      { value: 95.53, radius: 3 },
    ];

    const setYb = [
      { value: 4.84, radius: 3 },
      { value: 10.88, radius: 3 },
      { value: 16.81, radius: 3 },
      { value: 22.85, radius: 3 },
      { value: 28.78, radius: 3 },
      { value: 35.19, radius: 3 },
      { value: 41.11, radius: 3 },
      { value: 47.04, radius: 3 },
      { value: 52.84, radius: 3 },
      { value: 59.13, radius: 3 },
      { value: 65.42, radius: 3 },
      { value: 71.22, radius: 3 },
      { value: 77.39, radius: 3 },
      { value: 83.19, radius: 3 },
      { value: 89.48, radius: 3 },
      { value: 95.53, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p17 = defaultShape.extend({

  NAME: "terminals__DART__p17",

  init: function (attr) {
    const realWidth = 88.16;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p17/icon.png?itok=2e5hn8rd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 4.22, radius: 3 },
      { value: 10.03, radius: 3 },
      { value: 15.74, radius: 3 },
      { value: 21.44, radius: 3 },
      { value: 27.14, radius: 3 },
      { value: 32.84, radius: 3 },
      { value: 38.43, radius: 3 },
      { value: 44.24, radius: 3 },
      { value: 49.94, radius: 3 },
      { value: 55.76, radius: 3 },
      { value: 61.46, radius: 3 },
      { value: 67.05, radius: 3 },
      { value: 72.75, radius: 3 },
      { value: 78.68, radius: 3 },
      { value: 84.15, radius: 3 },
      { value: 89.85, radius: 3 },
      { value: 95.90, radius: 3 },
    ];

    const setYb = [
      { value: 4.22, radius: 3 },
      { value: 10.03, radius: 3 },
      { value: 15.74, radius: 3 },
      { value: 21.44, radius: 3 },
      { value: 27.14, radius: 3 },
      { value: 32.84, radius: 3 },
      { value: 38.43, radius: 3 },
      { value: 44.24, radius: 3 },
      { value: 49.94, radius: 3 },
      { value: 55.76, radius: 3 },
      { value: 61.46, radius: 3 },
      { value: 67.05, radius: 3 },
      { value: 72.75, radius: 3 },
      { value: 78.68, radius: 3 },
      { value: 84.15, radius: 3 },
      { value: 89.85, radius: 3 },
      { value: 95.90, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p18 = defaultShape.extend({

  NAME: "terminals__DART__p18",

  init: function (attr) {
    const realWidth = 93.24;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p18/icon.png?itok=1lZkzEEm",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 4.31, radius: 3 },
      { value: 9.60, radius: 3 },
      { value: 15.21, radius: 3 },
      { value: 20.17, radius: 3 },
      { value: 25.57, radius: 3 },
      { value: 31.18, radius: 3 },
      { value: 36.57, radius: 3 },
      { value: 42.07, radius: 3 },
      { value: 47.46, radius: 3 },
      { value: 52.97, radius: 3 },
      { value: 58.25, radius: 3 },
      { value: 63.54, radius: 3 },
      { value: 69.04, radius: 3 },
      { value: 74.54, radius: 3 },
      { value: 79.72, radius: 3 },
      { value: 85.22, radius: 3 },
      { value: 90.51, radius: 3 },
      { value: 96.01, radius: 3 },
    ];

    const setYb = [
      { value: 4.31, radius: 3 },
      { value: 9.60, radius: 3 },
      { value: 15.21, radius: 3 },
      { value: 20.17, radius: 3 },
      { value: 25.57, radius: 3 },
      { value: 31.18, radius: 3 },
      { value: 36.57, radius: 3 },
      { value: 42.07, radius: 3 },
      { value: 47.46, radius: 3 },
      { value: 52.97, radius: 3 },
      { value: 58.25, radius: 3 },
      { value: 63.54, radius: 3 },
      { value: 69.04, radius: 3 },
      { value: 74.54, radius: 3 },
      { value: 79.72, radius: 3 },
      { value: 85.22, radius: 3 },
      { value: 90.51, radius: 3 },
      { value: 96.01, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p19 = defaultShape.extend({

  NAME: "terminals__DART__p19",

  init: function (attr) {
    const realWidth = 93.32;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p19/icon.png?itok=x7vxuuPd",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 4.09, radius: 3 },
      { value: 9.11, radius: 3 },
      { value: 14.12, radius: 3 },
      { value: 19.34, radius: 3 },
      { value: 24.56, radius: 3 },
      { value: 29.48, radius: 3 },
      { value: 34.90, radius: 3 },
      { value: 39.61, radius: 3 },
      { value: 44.93, radius: 3 },
      { value: 50.15, radius: 3 },
      { value: 55.17, radius: 3 },
      { value: 60.29, radius: 3 },
      { value: 65.40, radius: 3 },
      { value: 70.73, radius: 3 },
      { value: 75.84, radius: 3 },
      { value: 80.86, radius: 3 },
      { value: 85.98, radius: 3 },
      { value: 91.10, radius: 3 },
      { value: 96.11, radius: 3 },
    ];

    const setYb = [
      { value: 4.09, radius: 3 },
      { value: 9.11, radius: 3 },
      { value: 14.12, radius: 3 },
      { value: 19.34, radius: 3 },
      { value: 24.56, radius: 3 },
      { value: 29.48, radius: 3 },
      { value: 34.90, radius: 3 },
      { value: 39.61, radius: 3 },
      { value: 44.93, radius: 3 },
      { value: 50.15, radius: 3 },
      { value: 55.17, radius: 3 },
      { value: 60.29, radius: 3 },
      { value: 65.40, radius: 3 },
      { value: 70.73, radius: 3 },
      { value: 75.84, radius: 3 },
      { value: 80.86, radius: 3 },
      { value: 85.98, radius: 3 },
      { value: 91.10, radius: 3 },
      { value: 96.11, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p20 = defaultShape.extend({

  NAME: "terminals__DART__p20",

  init: function (attr) {
    const realWidth = 103.4;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p20/icon.png?itok=43aQjoEb",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 3.70, radius: 3},
{value: 8.67, radius: 3},
{value: 13.53, radius: 3},
{value: 18.60, radius: 3},
{value: 23.27, radius: 3},
{value: 27.95, radius: 3},
{value: 32.81, radius: 3},
{value: 37.78, radius: 3},
{value: 42.94, radius: 3},
{value: 47.52, radius: 3},
{value: 52.58, radius: 3},
{value: 57.35, radius: 3},
{value: 62.41, radius: 3},
{value: 66.99, radius: 3},
{value: 72.05, radius: 3},
{value: 76.92, radius: 3},
{value: 81.89, radius: 3},
{value: 86.66, radius: 3},
{value: 91.63, radius: 3},
{value: 96.30, radius: 3},
    ];

    const setYb = [
      {value: 3.70, radius: 3},
      {value: 8.67, radius: 3},
      {value: 13.53, radius: 3},
      {value: 18.60, radius: 3},
      {value: 23.27, radius: 3},
      {value: 27.95, radius: 3},
      {value: 32.81, radius: 3},
      {value: 37.78, radius: 3},
      {value: 42.94, radius: 3},
      {value: 47.52, radius: 3},
      {value: 52.58, radius: 3},
      {value: 57.35, radius: 3},
      {value: 62.41, radius: 3},
      {value: 66.99, radius: 3},
      {value: 72.05, radius: 3},
      {value: 76.92, radius: 3},
      {value: 81.89, radius: 3},
      {value: 86.66, radius: 3},
      {value: 91.63, radius: 3},
      {value: 96.30, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p21 = defaultShape.extend({

  NAME: "terminals__DART__p21",

  init: function (attr) {
    const realWidth = 113.56;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p21/icon.png?itok=vifW8L5P",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 3.62, radius: 3 },
      { value: 8.17, radius: 3 },
      { value: 12.81, radius: 3 },
      { value: 17.64, radius: 3 },
      { value: 22.38, radius: 3 },
      { value: 26.74, radius: 3 },
      { value: 31.38, radius: 3 },
      { value: 36.21, radius: 3 },
      { value: 40.67, radius: 3 },
      { value: 45.31, radius: 3 },
      { value: 50.05, radius: 3 },
      { value: 54.87, radius: 3 },
      { value: 59.33, radius: 3 },
      { value: 63.97, radius: 3 },
      { value: 68.62, radius: 3 },
      { value: 73.17, radius: 3 },
      { value: 77.81, radius: 3 },
      { value: 82.54, radius: 3 },
      { value: 87.09, radius: 3 },
      { value: 91.74, radius: 3 },
      { value: 96.47, radius: 3 },
    ];

    const setYb = [
      { value: 3.62, radius: 3 },
      { value: 8.17, radius: 3 },
      { value: 12.81, radius: 3 },
      { value: 17.64, radius: 3 },
      { value: 22.38, radius: 3 },
      { value: 26.74, radius: 3 },
      { value: 31.38, radius: 3 },
      { value: 36.21, radius: 3 },
      { value: 40.67, radius: 3 },
      { value: 45.31, radius: 3 },
      { value: 50.05, radius: 3 },
      { value: 54.87, radius: 3 },
      { value: 59.33, radius: 3 },
      { value: 63.97, radius: 3 },
      { value: 68.62, radius: 3 },
      { value: 73.17, radius: 3 },
      { value: 77.81, radius: 3 },
      { value: 82.54, radius: 3 },
      { value: 87.09, radius: 3 },
      { value: 91.74, radius: 3 },
      { value: 96.47, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p22 = defaultShape.extend({

  NAME: "terminals__DART__p22",

  init: function (attr) {
    const realWidth = 72.92;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p22/icon.png?itok=KwzvyIP6",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 3.46, radius: 3 },
      { value: 7.99, radius: 3 },
      { value: 12.42, radius: 3 },
      { value: 16.68, radius: 3 },
      { value: 21.21, radius: 3 },
      { value: 25.73, radius: 3 },
      { value: 30.17, radius: 3 },
      { value: 34.52, radius: 3 },
      { value: 39.04, radius: 3 },
      { value: 43.39, radius: 3 },
      { value: 47.74, radius: 3 },
      { value: 52.26, radius: 3 },
      { value: 56.97, radius: 3 },
      { value: 61.14, radius: 3 },
      { value: 65.66, radius: 3 },
      { value: 70.01, radius: 3 },
      { value: 74.36, radius: 3 },
      { value: 78.88, radius: 3 },
      { value: 83.32, radius: 3 },
      { value: 87.76, radius: 3 },
      { value: 92.28, radius: 3 },
      { value: 96.72, radius: 3 },
    ];

    const setYb = [
      { value: 3.46, radius: 3 },
      { value: 7.99, radius: 3 },
      { value: 12.42, radius: 3 },
      { value: 16.68, radius: 3 },
      { value: 21.21, radius: 3 },
      { value: 25.73, radius: 3 },
      { value: 30.17, radius: 3 },
      { value: 34.52, radius: 3 },
      { value: 39.04, radius: 3 },
      { value: 43.39, radius: 3 },
      { value: 47.74, radius: 3 },
      { value: 52.26, radius: 3 },
      { value: 56.97, radius: 3 },
      { value: 61.14, radius: 3 },
      { value: 65.66, radius: 3 },
      { value: 70.01, radius: 3 },
      { value: 74.36, radius: 3 },
      { value: 78.88, radius: 3 },
      { value: 83.32, radius: 3 },
      { value: 87.76, radius: 3 },
      { value: 92.28, radius: 3 },
      { value: 96.72, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p23 = defaultShape.extend({

  NAME: "terminals__DART__p23",

  init: function (attr) {
    const realWidth = 118.64;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p23/icon.png?itok=a_a_7slv",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      { value: 3.40, radius: 3 },
      { value: 7.48, radius: 3 },
      { value: 11.81, radius: 3 },
      { value: 16.06, radius: 3 },
      { value: 20.39, radius: 3 },
      { value: 24.47, radius: 3 },
      { value: 28.80, radius: 3 },
      { value: 33.05, radius: 3 },
      { value: 37.38, radius: 3 },
      { value: 41.55, radius: 3 },
      { value: 45.79, radius: 3 },
      { value: 50.04, radius: 3 },
      { value: 54.38, radius: 3 },
      { value: 58.62, radius: 3 },
      { value: 62.96, radius: 3 },
      { value: 67.12, radius: 3 },
      { value: 71.11, radius: 3 },
      { value: 75.53, radius: 3 },
      { value: 79.86, radius: 3 },
      { value: 84.03, radius: 3 },
      { value: 88.45, radius: 3 },
      { value: 92.61, radius: 3 },
      { value: 96.77, radius: 3 },
    ];

    const setYb = [
      { value: 3.40, radius: 3 },
      { value: 7.48, radius: 3 },
      { value: 11.81, radius: 3 },
      { value: 16.06, radius: 3 },
      { value: 20.39, radius: 3 },
      { value: 24.47, radius: 3 },
      { value: 28.80, radius: 3 },
      { value: 33.05, radius: 3 },
      { value: 37.38, radius: 3 },
      { value: 41.55, radius: 3 },
      { value: 45.79, radius: 3 },
      { value: 50.04, radius: 3 },
      { value: 54.38, radius: 3 },
      { value: 58.62, radius: 3 },
      { value: 62.96, radius: 3 },
      { value: 67.12, radius: 3 },
      { value: 71.11, radius: 3 },
      { value: 75.53, radius: 3 },
      { value: 79.86, radius: 3 },
      { value: 84.03, radius: 3 },
      { value: 88.45, radius: 3 },
      { value: 92.61, radius: 3 },
      { value: 96.77, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__DART__p24 = defaultShape.extend({

  NAME: "terminals__DART__p24",

  init: function (attr) {
    const realWidth = 123.72;
    const realHeight = 34.66;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/DART/p24/icon.png?itok=rSyRJWxX",
      realWidth,
      realHeight,
    }, attr));


    const setYt = [
      {value: 3.26, radius: 3},
      {value: 7.33, radius: 3},
      {value: 11.33, radius: 3},
      {value: 15.32, radius: 3},
      {value: 19.48, radius: 3},
      {value: 23.63, radius: 3},
      {value: 27.71, radius: 3},
      {value: 31.62, radius: 3},
      {value: 35.78, radius: 3},
      {value: 39.85, radius: 3},
      {value: 44.09, radius: 3},
      {value: 48.17, radius: 3},
      {value: 52.00, radius: 3},
      {value: 56.15, radius: 3},
      {value: 60.31, radius: 3},
      {value: 64.47, radius: 3},
      {value: 68.38, radius: 3},
      {value: 72.37, radius: 3},
      {value: 76.53, radius: 3},
      {value: 80.60, radius: 3},
      {value: 84.76, radius: 3},
      {value: 88.75, radius: 3},
      {value: 92.58, radius: 3},
      {value: 97.07, radius: 3},
    ];

    const setYb = [
      {value: 3.26, radius: 3},
      {value: 7.33, radius: 3},
      {value: 11.33, radius: 3},
      {value: 15.32, radius: 3},
      {value: 19.48, radius: 3},
      {value: 23.63, radius: 3},
      {value: 27.71, radius: 3},
      {value: 31.62, radius: 3},
      {value: 35.78, radius: 3},
      {value: 39.85, radius: 3},
      {value: 44.09, radius: 3},
      {value: 48.17, radius: 3},
      {value: 52.00, radius: 3},
      {value: 56.15, radius: 3},
      {value: 60.31, radius: 3},
      {value: 64.47, radius: 3},
      {value: 68.38, radius: 3},
      {value: 72.37, radius: 3},
      {value: 76.53, radius: 3},
      {value: 80.60, radius: 3},
      {value: 84.76, radius: 3},
      {value: 88.75, radius: 3},
      {value: 92.58, radius: 3},
      {value: 97.07, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});
 const terminals__phoenix_contact__MBKKB_2_5 = defaultShape.extend({

  NAME: "terminals__phoenix_contact__MBKKB_2_5",

  init: function (attr) {
    const realWidth = 7.7;
    const realHeight = 62.5;

    this.cloneWidth = 5.2;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/phoenix_contact/MBKKB_2_5/icon.png?itok=ppLN1gqU",
      realWidth,
      realHeight,
    }, attr));

    const y1 = realHeight * Utils.sizeCoef * 34.38 / 100;
    const y2 = realHeight * Utils.sizeCoef * 64.06 / 100;
    const y3 = realHeight * Utils.sizeCoef * 64.06 / 100;
    const y4 = realHeight * Utils.sizeCoef * 34.38 / 100;

    const x1 = realWidth * Utils.sizeCoef * 14.68 / 100;
    const x2 = realWidth * Utils.sizeCoef * 46.17 / 100;
    const x3 = realWidth * Utils.sizeCoef * 66.8 / 100;
    const x4 = realWidth * Utils.sizeCoef * 85.53 / 100;

    const radius = 3 * Utils.sizeCoef;

    Utils.createPort(this, { locatorAttr: { x: x1, y: y1 }, direction: 0, width: radius, height: radius });
    Utils.createPort(this, { locatorAttr: { x: x2, y: y2 }, direction: 0, width: radius, height: radius });
    Utils.createPort(this, { locatorAttr: { x: x3, y: y3 }, direction: 2, width: radius, height: radius });
    Utils.createPort(this, { locatorAttr: { x: x4, y: y4 }, direction: 2, width: radius, height: radius });

  },
});
 const terminals__phoenix_contact__PTTB_2_5 = defaultShape.extend({

  NAME: "terminals__phoenix_contact__PTTB_2_5",


  init: function (attr) {
    const realWidth = 5.2;
    const realHeight = 98;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/phoenix_contact/PTTB_2_5/icon.png?itok=A-qMT73x",
      realWidth,
      realHeight,
    }, attr));

    const y = realHeight * Utils.sizeCoef * 50 / 100;

    const x1 = realWidth * Utils.sizeCoef * 7.92 / 100;
    const x2 = realWidth * Utils.sizeCoef * 35.02 / 100;
    const x3 = realWidth * Utils.sizeCoef * 71.09 / 100;
    const x4 = realWidth * Utils.sizeCoef * 92.94 / 100;

    const radius = 3 * Utils.sizeCoef;

    Utils.createPort(this, {locatorAttr: {x: x1, y}, direction: 0, width: radius, height: radius});
    Utils.createPort(this, {locatorAttr: {x: x2, y}, direction: 0, width: radius, height: radius});
    Utils.createPort(this, {locatorAttr: {x: x3, y}, direction: 2, width: radius, height: radius});
    Utils.createPort(this, {locatorAttr: {x: x4, y}, direction: 2, width: radius, height: radius});

  },
});
 const terminals__phoenix_contact__PT_2_5 = defaultShape.extend({

  NAME: "terminals__phoenix_contact__PT_2_5",


  init: function (attr) {
    const realWidth = 5.2;
    const realHeight = 48.6;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/terminals/phoenix_contact/PT_2_5/icon.png?itok=1aG5GzJ7",
      realWidth,
      realHeight,
    }, attr));

    const y = realHeight * Utils.sizeCoef * 50 / 100;

    const x1 = realWidth * Utils.sizeCoef * 11.6 / 100;
    const x2 = realWidth * Utils.sizeCoef * 88.53 / 100;

    const radius = 3 * Utils.sizeCoef;

    Utils.createPort(this, {locatorAttr: {x: x1, y}, direction: 0, width: radius, height: radius});
    Utils.createPort(this, {locatorAttr: {x: x2, y}, direction: 2, width: radius, height: radius});

  },
});
 const wiren_board__io__WBIO_AI_DV_12 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_AI_DV_12",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_AI_DV_12/icon.png?itok=zzUgNJns",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.55, radius: 3 },
      { value: 16.18, radius: 3 },
      { value: 22.82, radius: 3 },
      { value: 29.45, radius: 3 },
      { value: 40.13, radius: 3 },
      { value: 46.76, radius: 3 },
      { value: 53.40, radius: 3 },
      { value: 60.03, radius: 3 },
      { value: 70.71, radius: 3 },
      { value: 77.51, radius: 3 },
      { value: 84.14, radius: 3 },
      { value: 90.78, radius: 3 },
    ];

    const setYb = [
      { value: 9.55, radius: 3 },
      { value: 16.18, radius: 3 },
      { value: 22.82, radius: 3 },
      { value: 29.45, radius: 3 },
      { value: 40.13, radius: 3 },
      { value: 46.76, radius: 3 },
      { value: 53.40, radius: 3 },
      { value: 60.03, radius: 3 },
      { value: 70.71, radius: 3 },
      { value: 77.51, radius: 3 },
      { value: 84.14, radius: 3 },
      { value: 90.78, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_AI_DV_12_4_20ma = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_AI_DV_12_4_20ma",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_AI_DV_12_4_20ma/icon.png?itok=zib7YPca",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 9.55, radius: 3 },
      { value: 16.18, radius: 3 },
      { value: 22.82, radius: 3 },
      { value: 29.45, radius: 3 },
      { value: 40.13, radius: 3 },
      { value: 46.76, radius: 3 },
      { value: 53.40, radius: 3 },
      { value: 60.03, radius: 3 },
      { value: 70.71, radius: 3 },
      { value: 77.51, radius: 3 },
      { value: 84.14, radius: 3 },
      { value: 90.78, radius: 3 },
    ];

    const setYb = [
      { value: 9.55, radius: 3 },
      { value: 16.18, radius: 3 },
      { value: 22.82, radius: 3 },
      { value: 29.45, radius: 3 },
      { value: 40.13, radius: 3 },
      { value: 46.76, radius: 3 },
      { value: 53.40, radius: 3 },
      { value: 60.03, radius: 3 },
      { value: 70.71, radius: 3 },
      { value: 77.51, radius: 3 },
      { value: 84.14, radius: 3 },
      { value: 90.78, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_AO_10V_8 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_AO_10V_8",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_AO_10V_8/icon.png?itok=uoRT_PlM",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];

    const setYb = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DI_HVD_8 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DI_HVD_8",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DI_HVD_8/icon.png?itok=wJw7KM60",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];

    const setYb = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DI_HVD_16 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DI_HVD_16",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DI_HVD_16/icon.png?itok=PoK_wCTV",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];

    const setYb = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DI_WD_14 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DI_WD_14",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DI_WD_14/icon.png?itok=Fazd2ymb",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];

    const setYb = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DO_R1G_16 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DO_R1G_16",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DO_R1G_16/icon.png?itok=fN4_5x49",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];

    const setYb = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DO_R10A_8 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DO_R10A_8",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DO_R10A_8/icon.png?itok=_Hk6m5lL",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];

    const setYb = [
      { value: 11.33, radius: 3 },
      { value: 20.87, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.16, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.58, radius: 3 },
      { value: 79.13, radius: 3 },
      { value: 89.00, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DO_R10R_4 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DO_R10R_4",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DO_R10R_4/icon.png?itok=ozPulNnf",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 12.64, radius: 3 },
      { value: 22.37, radius: 3 },
      { value: 32.09, radius: 3 },
      { value: 41.82, radius: 3 },
      { value: 60.62, radius: 3 },
      { value: 70.34, radius: 3 },
      { value: 80.23, radius: 3 },
      { value: 89.79, radius: 3 },
    ];

    const setYb = [
      { value: 12.64, radius: 3 },
      { value: 22.37, radius: 3 },
      { value: 32.09, radius: 3 },
      { value: 41.82, radius: 3 },
      { value: 60.62, radius: 3 },
      { value: 70.34, radius: 3 },
      { value: 80.23, radius: 3 },
      { value: 89.79, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__io__WBIO_DO_SSR_8 = defaultShape.extend({

  NAME: "wiren_board__io__WBIO_DO_SSR_8",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/io/WBIO_DO_SSR_8/icon.png?itok=qB40pvEg",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];

    const setYb = [
      { value: 14.95, radius: 3 },
      { value: 24.77, radius: 3 },
      { value: 34.35, radius: 3 },
      { value: 43.93, radius: 3 },
      { value: 57.48, radius: 3 },
      { value: 67.29, radius: 3 },
      { value: 76.87, radius: 3 },
      { value: 86.45, radius: 3 },
    ];


    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__meters__WB_MAP3E = defaultShape.extend({

  NAME: "wiren_board__meters__WB_MAP3E",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/meters/WB_MAP3E/icon.png?itok=5Fq6eiY9",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 11.33, radius: 3 },
      { value: 21.04, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.32, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.42, radius: 3 },
      { value: 79.29, radius: 3 },
      { value: 88.83, radius: 3 },
    ];

    const setYb = [
      { value: 10.19, radius: 3 },
      { value: 16.99, radius: 3 },
      { value: 23.62, radius: 3 },
      { value: 30.26, radius: 3 },
      { value: 37.06, radius: 3 },
      { value: 43.69, radius: 3 },
      { value: 68.77, radius: 3 },
      { value: 75.57, radius: 3 },
      { value: 82.04, radius: 3 },
      { value: 88.83, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__meters__WB_MAP3ET = defaultShape.extend({

  NAME: "wiren_board__meters__WB_MAP3ET",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/meters/WB_MAP3ET/icon.png?itok=0efwZEEO",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 11.33, radius: 3 },
      { value: 21.04, radius: 3 },
      { value: 30.74, radius: 3 },
      { value: 40.45, radius: 3 },
      { value: 50.32, radius: 3 },
      { value: 59.87, radius: 3 },
      { value: 69.42, radius: 3 },
      { value: 79.29, radius: 3 },
      { value: 88.83, radius: 3 },
    ];

    const setYb = [
      { value: 10.19, radius: 3 },
      { value: 16.99, radius: 3 },
      { value: 23.62, radius: 3 },
      { value: 30.26, radius: 3 },
      { value: 37.06, radius: 3 },
      { value: 43.69, radius: 3 },
      { value: 68.77, radius: 3 },
      { value: 75.57, radius: 3 },
      { value: 82.04, radius: 3 },
      { value: 88.83, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__meters__WB_MAP3EV = defaultShape.extend({

  NAME: "wiren_board__meters__WB_MAP3EV",

  init: function (attr) {
    const realWidth = 36;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/meters/WB_MAP3EV/icon.png?itok=swjmW3Ki",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      {value: 29.21, radius: 3},
      {value: 42.99, radius: 3},
      {value: 57.24, radius: 3},
      {value: 71.26, radius: 3},
    ];

    const setYb = [
      {value: 56.31, radius: 3},
      {value: 66.12, radius: 3},
      {value: 75.70, radius: 3},
      {value: 85.28, radius: 3},
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__meters__WB_MAP6S = defaultShape.extend({

  NAME: "wiren_board__meters__WB_MAP6S",

  init: function (attr) {
    const realWidth = 53;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/meters/WB_MAP6S/icon.png?itok=Gq2YAhLE",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 10.52, radius: 3 },
      { value: 17.15, radius: 3 },
      { value: 23.95, radius: 3 },
      { value: 30.58, radius: 3 },
      { value: 37.38, radius: 3 },
      { value: 44.01, radius: 3 },
      { value: 59.39, radius: 3 },
      { value: 69.09, radius: 3 },
      { value: 78.80, radius: 3 },
      { value: 88.51, radius: 3 },
    ];

    const setYb = [
      { value: 10.52, radius: 3 },
      { value: 17.15, radius: 3 },
      { value: 23.95, radius: 3 },
      { value: 30.58, radius: 3 },
      { value: 37.38, radius: 3 },
      { value: 44.01, radius: 3 },
      { value: 69.09, radius: 3 },
      { value: 75.73, radius: 3 },
      { value: 82.36, radius: 3 },
      { value: 89.16, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


 const wiren_board__meters__WB_MAP12E = defaultShape.extend({

  NAME: "wiren_board__meters__WB_MAP12E",

  init: function (attr) {
    const realWidth = 106;
    const realHeight = 90;

    this._super($.extend({
      path: "/sites/default/files/styles/watermark/module/el_elements/elements/wirenBoard/meters/WB_MAP12E/icon.png?itok=jreAKNWR",
      realWidth,
      realHeight,
    }, attr));

    const setYt = [
      { value: 5.10, radius: 3 },
      { value: 8.44, radius: 3 },
      { value: 11.78, radius: 3 },
      { value: 15.05, radius: 3 },
      { value: 18.31, radius: 3 },
      { value: 21.66, radius: 3 },
      { value: 28.03, radius: 3 },
      { value: 31.29, radius: 3 },
      { value: 34.55, radius: 3 },
      { value: 37.82, radius: 3 },
      { value: 41.08, radius: 3 },
      { value: 44.43, radius: 3 },
      { value: 55.41, radius: 3 },
      { value: 60.19, radius: 3 },
      { value: 64.89, radius: 3 },
      { value: 69.59, radius: 3 },
      { value: 74.44, radius: 3 },
      { value: 79.30, radius: 3 },
      { value: 84.08, radius: 3 },
      { value: 88.77, radius: 3 },
      { value: 93.55, radius: 3 }
    ];

    const setYb = [
      { value: 5.18, radius: 3 },
      { value: 8.44, radius: 3 },
      { value: 11.78, radius: 3 },
      { value: 15.05, radius: 3 },
      { value: 18.31, radius: 3 },
      { value: 21.58, radius: 3 },
      { value: 27.95, radius: 3 },
      { value: 31.29, radius: 3 },
      { value: 34.55, radius: 3 },
      { value: 37.82, radius: 3 },
      { value: 41.16, radius: 3 },
      { value: 44.43, radius: 3 },
      { value: 83.76, radius: 3 },
      { value: 87.10, radius: 3 },
      { value: 90.37, radius: 3 },
      { value: 93.63, radius: 3 },
    ];

    Utils.createSetOfPorts(this, setYt, realWidth, realHeight, 0);
    Utils.createSetOfPorts(this, setYb, realWidth, realHeight, 2);
  },
});


